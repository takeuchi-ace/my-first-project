# イベントテキスト再監査レポート（第2版）

**作成日**: 2026-07-25
**対象**: enjoy-golf 全イベントデータ（現行）
**ステータス**: レポートのみ（コード変更なし）
**前版**: `AUDIT_EVENT_QA.md`（2026-02-22）

## 監査方法

1. イベントデータ全ファイルを精読（events / curated / character / lunch / female / extreme / goodBack / tensionBack / ace / competition / morningShot / putting）
2. データを実際に require し、本番と同じ経路（`applyChoice` → trait補正 → キャラreactions → likes/hates → クランプ → `evaluateReactionRank`）で全1,160選択肢 × 21キャラをシミュレーション
3. 到達可否・重複・tag被覆・選択肢位置の偏りを機械集計

**未実施**: 実機プレイによる目視確認。表示崩れ・演出タイミングは本レポートの対象外。

---

## 0. 前版との差分（前提の訂正）

### 件数が違う

| | 前版の記載 | 実測 |
|---|---|---|
| events.ts | 154 | **164** |
| characterEvents.ts | 42 | **107** |
| lunchEvents.ts | 21 | 21 |
| 合計 | 309 | **372**（選択肢 1,160） |

前版の154は `events.ts` 末尾の統計コメント（「既存76＋新規78＝154」）を転記したもの。同コメント内のstage別内訳を合計すると164で、コメント自体が内部矛盾している。`characterEvents.ts` のコメントも「各キャラ5本×21＝105本」だが、キャラ1・2だけ6本あり実測107本。`lunchEvents.ts` のコメントは「16本」だが実測21本。

### 前版のNG14件はほぼ解消済み

| 旧NG | 内容 | 現状 |
|---|---|---|
| NG-1 caddy_tip | 主語ズレ | **緩和**（descriptionに「あなたはどう振る舞う？」追記）。ただし「相手のクラブを拭いている」のに自分がお礼/チップという捻れは残存 |
| NG-2 opponent_ball_mark | 3択とも「直す」 | **解消**（直す／指摘する／空気で伝える に差別化） |
| NG-3 caddy_ball_advice | 3択とも「渡す」 | **未修正** |
| NG-4 drunk_praise | 判断トリガーなし | **緩和**（「あなたはどう振る舞う？」追記） |
| NG-5〜11 curated speech | 内心独白 | **概ね解消**（「ありがとう、ちょうどいい距離感だね。」等、口語に書き換え済み） |

前版の「NG率4.5%、95%以上は問題なし」という結論は、**prompt→choices→speech のQ/A整合という1軸のみ**で測ったもの。今回はプレイヤーに見える表示・キャラ一貫性・到達可否・選択の設計まで含めたため、評価が大きく変わる。

---

## A. 表示が壊れている（プレイヤーに直接見える）— 優先度: 最高

### A-1. 「○○さん」が50箇所そのまま表示される

置換処理が**コードベースに存在しない**。`src` 全体の `.replace()` はナビゲーションと色変換のみで、キャラ名を差し込む処理は一つもない。

| ファイル | 箇所 |
|---|---|
| events.ts | 41 |
| goodBackEvents.ts | 4 |
| characterEvents.ts | 2 |
| tensionBackEvents.ts | 2 |
| femaleEvents.ts | 1 |
| **合計** | **50** |

実際に表示される例:
- 「○○さんこそ、相変わらずお忙しいですか？」（locker_room）
- 「○○さんの経営理念、本にしてほしいです！」（drunk_praise）
- 「○○さんが特別ですよ」（tension_back_probing）

### A-2. fullName の「恒一」が13人分重複し、結果画面に表示される

`fullName` は [ResultScreenSimple.tsx:115](src/screens/ResultScreenSimple.tsx:115)（`{character.fullName} さん`）と [ProfileScreen.tsx:105](src/screens/ProfileScreen.tsx:105) でプレイヤーに表示される。

「恒一」を持つキャラ: 三浦（坊っちゃん）・黒田・光山・巌・中村・佐藤・松本・大門・星野・金城・白石・篠原・鷹宮 = **13人**。テンプレのコピペが残ったものと見られる。結果画面は毎ラウンド必ず通る画面。

### A-3. 銀座ハジメの fullName が氏名でない

`fullName: '弁護士法人エース 代表弁護士'` のため、結果画面に「弁護士法人エース 代表弁護士 さん」と表示される。

### A-4. 同一キャラの呼称が混在

| キャラ | 表示名 | fullName | 本文中の呼称 |
|---|---|---|---|
| 15 | 千鶴 | 藤原 千鶴 | 千鶴／**藤原**（lunchEvents のコメント見出しも「老舗料亭女将・藤原」） |
| 21 | ミツキ | 立花 美月 | ミツキ／**立花** |

キャラ21は `reactionLines` が全て「立花が〜」、`char_21_3`/`char_21_5` は「ミツキが〜」「ミツキさん」、`char_21_2`/`lunch_21` は「立花さん」。同一ラウンド内で二つの名前で呼ばれる。

---

## B. 書いたのに出ない（デッドコンテンツ）— 優先度: 高

### B-1. `aceEvents.ts`（20イベント・60応答・296行）が完全に未参照

`src` 全体で `aceEvents` を import している箇所は**ゼロ**。実際に動いているACE相談は `aceConsults.ts`（`aceEngine.ts` が import）で、こちらは悩み／回答のペア形式。`aceEvents.ts` は置き換えられた旧稿が消されずに残ったもの。

### B-2. `lunchMenu.ts`（39行・メニュー22品）も未参照

`lunchMiniGame.ts` が別途メニューを持っているため未使用。

### B-3. stage 1 と stage 5 のイベントは1件も出ない

各ホールは1イベントで消費され（[engine.ts:485](src/logic/engine.ts:485)）、抽選は `stage === currentHole` を優先する（[engine.ts:312](src/logic/engine.ts:312)）。ところが:

| ホール | 固定で埋まるもの | stage一致イベントの運命 |
|---|---|---|
| 1 | 朝イチのショット（必ず） | **stage 1 の22件が永久に出ない** |
| 5 | ランチイベント（21キャラ全員に定義あり） | **stage 5 の23件が永久に出ない** |
| 9 | 最終パット（ACEラウンド以外は必ず） | stage 9 の19件はACEラウンドのみ |

stage不一致時のフォールバックは、同ステージ在庫が約20件あるため発火しない。**合計45件が完全到達不能、19件が条件付き**。

### B-4. 厳選イベント16件のうち6件が上記で死蔵

- stage 1（4件）: `c_parking_greeting` `c_first_tee` `c_praise_shot` `c_your_miss`
- stage 9（2件）: `c_sunset_hole` `c_final_putt`

`curatedEvents.ts` 冒頭コメントは「front: stage 1-2（8件）→4件抽選」と書いているが、実際に抽選対象になるのは stage 2 の4件のみ。**最も手のかかった、唯一rank別セリフを持つイベント群の3分の1が死んでいる**。

### B-5. `events.ts` に厳選イベントの劣化コピーが3件

description が完全一致、選択肢文言もほぼ一致。curated版はrank別セリフ付きの上位互換。

| events.ts | curated |
|---|---|
| `first_tee_tension` | `c_first_tee` |
| `praise_shot` | `c_praise_shot` |
| `parking_greeting` | `c_parking_greeting` |

いずれも stage 1 なので**両方とも死蔵**。

### B-6. goodBack / tensionBack の `stage` は engine が参照していない

後半のmood分岐（[engine.ts:268-288](src/logic/engine.ts:268)）はプールから無条件randomで引く。`stage: 9` と書かれた `tension_back_score_doubt` はホール6でも出る。データが実装と噛み合っておらず、読む側を誤らせる。

### B-7. 手書きrank別セリフ192行のうち60行（31%）が到達不能

curated 16件×3択×4rank＝192行。21キャラ横断でシミュレーションしても発生しないrankがあり、そのセリフは絶対に表示されない。

例: `c_parking_greeting` choice3「お車素敵ですね」は good が出ない／choice1「深々お辞儀」は bad が出ない。

---

## C. キャラが立っていない — 優先度: 高

### C-1. 毎ラウンド必ず通るホール1が、19キャラで汎用文

朝イチのショットは全ラウンドのホール1で固定発生するが、`morningShotEvents.ts` に個別データがあるのは**田中と鬼塚の2キャラだけ**。残り19キャラは `buildGenericMorningShotEvent` にフォールバックし、

> 「相手は見事なショットを放った。ボールはフェアウェイ中央へ飛んでいく。」

と表示される。**ラウンドの第一印象が、19人中19人で同じ無名の文章**になる。

### C-2. ホール9の最終パットも同じ構造

`puttingEvent.ts` も田中・鬼塚のみ個別プロフィール。19キャラは `GENERIC_PROFILE` の「…ナイスパット」と相手が頷いた。／相手が何も言わず前を向いた。 になる。

### C-3. 厳選イベントのセリフが全キャラ同じ口調

[GameScreenSimple.tsx:571](src/screens/GameScreenSimple.tsx:571) は `choice.speech` を最優先で使い、キャラの `speechStyle` を無視する。curated のセリフは男性のカジュアル口調（「ハハハ！それ聞いたら気が楽になった！」「いいね！その意気だ！」「〜だね」）で固定されているため:

| キャラ | 本来の口調（speechStyle） | curatedで実際に喋る言葉 |
|---|---|---|
| 千鶴（S12・料亭女将） | 「よろしければ、少し召し上がります？」 | 「ハハハ！それ聞いたら気が楽になった！」 |
| 巌（S07・昭和の重鎮・一人称わし） | 「礼に始まり礼に終わる。」 | 「いいね！その意気だ！」 |
| スミス（S05・外資・一人称I） | 「That makes sense.」 | 「そうだね。冷静なアドバイスありがとう。」 |
| 金城（S17・関西弁） | 「攻めなあかんやろ。」 | 同上 |

最も品質の高いイベント群が、同時に最もキャラを壊している。

### C-4. speechStyleId が3組重複

| style | 共有しているキャラ |
|---|---|
| S03 | 二代目オーナー・坊っちゃん / **エース弁護士・銀座ハジメ** |
| S08 | テック社長・中村 / IT起業家・篠原 |
| S11 | 政界フィクサー・大門 / 重工会長・鷹宮 |

21キャラに対しスタイルは18種。ラスボスの銀座ハジメが坊っちゃんと同じ汎用セリフを話す。

### C-5. セリフ解像度は全体の4%

| セリフの持ち方 | choice数 | 割合 |
|---|---|---|
| rank別4段階（`speech`） | 48 | 4.1% |
| `speechOverride`（データ側での指定） | 0 | 0% |
| **なし → 汎用テンプレへ** | **1,112** | **95.9%** |

1,112選択肢では、何を選んでも `speechLineTemplates[styleId][rank]` からランダムに1行引くだけ。選択内容とセリフが結びつかない（「ま、そういう手もあるか。」等）。`Choice.speech` の型は用意されているので、データを書けば効く。

### C-6. 主人公の一人称が混在

| 一人称 | 箇所 |
|---|---|
| 自分 | 78 |
| 僕 | 10（femaleEvents 7・characterEvents 2・goodBack 1） |
| 俺 | 5（`c_water_hazard`「俺も池に入れたことありますよ」、`pond_dive`「俺が念力で」等） |
| 私 | 5（`tension_back_payment`「ここは私が」等） |

同一プレイヤーが場面によって自分／僕／俺／私を使い分けている。

### C-7. 共通プールは相手を名前で呼ばない（274箇所）

`events.ts` の description は「相手が」「相手の」で統一されている（274箇所）。一方 characterEvents は「田中がスコアカードを几帳面に記入している。」と実名。プレイヤーが遭遇する回数は共通プールの方が多いため、**大半の時間、対戦相手は「相手」と呼ばれ続ける**。A-1の「○○さん」と合わせて、名前を差し込む仕組みが無いことが根本原因。

---

## D. 選択が機能していない — 優先度: 高

### D-1/D-2. 正解と地雷の位置が固定されている

本番パイプラインで全キャラ分シミュレーションした「最良／最悪選択肢が何番目か」の分布:

| ファイル | 最良の位置 | 最悪の位置 |
|---|---|---|
| **characterEvents.ts** | **1番目 98%** | **3番目 93%** |
| **lunchEvents.ts** | **1番目 95%** | 3番目 81% |
| tensionBackEvents.ts | 1番目 84% | 3番目 92% |
| extremeEvents.ts | 1番目 78% | 3番目 85% |
| goodBackEvents.ts | 1番目 75% | 3番目 72% |
| **femaleEvents.ts** | **2番目 99%** | 1番目 84% |
| events.ts | 1番目 60% | 3番目 51% |
| curated(front) | 1番目 48% | 3番目 70% |
| curated(back) | 1番目 50% | 3番目 42% |

キャラ固有イベント107件は、**文章を読まず1番目を押すだけで105件が最良**。厳選イベントだけが50/50に近く、設計として健全。

### D-3. 見た目だけで地雷が判別できる

「！！」を含む、または45文字以上の選択肢43件のうち、**33件（77%）が最悪選択肢**。

選択肢の文字数と平均trust:

| 文字数 | 件数 | 平均trust |
|---|---|---|
| 〜14字 | 264 | +2.12 |
| 15-24字 | 565 | +3.62 |
| 25-34字 | 196 | +2.96 |
| 35-49字 | 46 | **+7.07** |
| 50字以上 | 29 | **−6.00** |

長文は「信頼回復系の正直な謝罪」か「！！連発の暴走」の両極に振れるが、後者は「！！」で見分けられる。

### D-4. femaleEvents 20件が完全に同一骨格

ファイル冒頭コメントが自ら明記している通り「A:媚び系 / B:誠実系 / C:共感系 / D:ズレ系」の4択が20件すべてで同じ順序。delta値も使い回し（媚び trust−2〜−4/creep6〜8、誠実 trust+6〜8/creep0、共感 trust+4/creep2、ズレ trust−4/creep4）。女性キャラ4人とのラウンドでは30%の確率で差し込まれるため、繰り返し遭遇する。

### D-5. events.ts の extreme 33件が固定テンプレ

「3番目の選択肢＝『〜！！』連発＋creep 12〜18＋trust負」。文体も「今すぐ〜します！！！」「もはや〜！！」でほぼ同型。

### D-6. 選択肢ラベルに評価的ト書きが混入し、失敗をネタバレしている

プレイヤーの発言ラベルの末尾に、ナレーターの評価が付いている:

- 「…と訳のわからない理屈で庇う」（`opponent_complains_course`）
- 「…と根拠なく断言する」（`tough_putt_read`）
- 「…と勝手に決める」（`opponent_eagle_chance`）
- 「…とバレる嘘をつく」（`partner_score_gap`）
- 「…と依存心全開で叫ぶ」（`next_round_planning`）
- 「…とおかしなことを叫ぶ」（`par3_hole_in_one_near`）

前版が指摘した「speechへの動作描写混入（#7）」の悪化版。選ぶ前に失敗だと分かるため、リスクを取る緊張感が消える。

### D-7. 選んでも反応が変わらないイベント15件

3択すべてが同rankに落ちるもの（序盤ゲージ・キャラ平均）:

`rain_start` `opponent_ball_mark` `beer_first` `opponent_good_mood` `award_ceremony` `car_send_off` `opponent_yips` `cart_path_navigation` `greenside_drama` `evening_chill` `taxi_call_help` `business_card_exchange` `c_rival_approach` `good_back_self_deprecation` `good_back_course_praise`（すべて neutral）

### D-8. キャラ差が出ない選択肢が17%

779選択肢中133件は、21キャラ全員で同じrankになる。特に lunchEvents は 36/63（57%）。

---

## E. ロジック不整合 — 優先度: 中

### E-1. 最終パットの「誤ったアドバイス」が機能しない

`puttingEvent.ts` は `adviceAccuracy` で正誤を振り分けるが、誤アドバイス時の文言が傾斜によって分岐していない:

- `ONIZUKA_PROFILE`: 誤アドバイスは常に「右に外してみては！勢いで決めろ！！」
- `GENERIC_PROFILE`（19キャラ）: 誤アドバイスは常に「右に外してみてはどうでしょう」

ところが slope `left` の `correctAim` は `right`。**傾斜が左のとき、誤アドバイスがそのまま正解になる**。鬼塚と汎用19キャラ、つまり20/21キャラで読み合いが成立しない。`TANAKA_PROFILE` は傾斜ごとに分岐しており問題なし。

### E-2. creep が二つの意味で使われている

本来「気持ち悪さ／距離感の失敗」を表すゲージだが、「場を冷やした」ペナルティとしても流用されている:

- 「…」と黙る → creep +9（`char_13_2`）
- 無言で目をそらす → creep +9（`char_10_5`）
- 「時間大丈夫ですか？」 → creep +8（`char_3_1`）
- 「科学的根拠はあるんですか？」 → creep +10（`char_6_5`）

プレイヤーには「なぜ黙ったら気持ち悪がられるのか」が伝わらない。ゲージ名と体験の乖離。

### E-3. worst の発生率が高い

| ファイル | worst率 |
|---|---|
| lunchEvents | 48.2% |
| tensionBackEvents | 47.8% |
| femaleEvents | 42.4% |
| characterEvents | 41.4% |
| events.ts | 23.6% |

キャラ固有・ランチという「そのキャラらしさが出る場面」ほど、選択肢の4割が最悪反応になる。

### E-4. 宣言済みtag 43種のうち4種が未使用

`self_suppress` `over_adjust` `over_flatter` `forced_laugh`（キャラ側の `reactions`/`likesTags`/`hatesTags` からも参照されていないため、実害はないが定義だけ残っている）

### E-5/E-6. 個別の数値不整合

- `tee_peg_pickup` の「気にしない」は `delta: {}`（全ゲージ0）。1,160選択肢中で唯一の完全無効選択肢（`aceEvents` の60件は設計上ゼロ、かつB-1で未使用）
- `rest_room_wait`「相手のスコアカードをこっそり確認する」は `tags: ['neutral']` なのに creep +4

---

## F. ゴルフとして成立しない — 優先度: 中

### F-1. 旗を動かしても不正にならない

`flag_attend_precise`「ショットのタイミングでそっと旗を動かしてピンの位置を相手に有利な方向へ示す」— 旗を動かしてもカップは動かない。creep +8 が架空の行為に付いている。

### F-2. カップは動かせない

`final_hole_birdie_attempt`「こっそりカップをわずかに有利な位置に調整しようとする」— カップは地面に埋設されたホールカップで、プレー中に位置を変えることは不可能。creep +18（データ中ほぼ最大値）が成立しない行為に付いている。

### F-3. 文意が破綻している

`distraction_noise`「身を張って相手の視界に入り、注意をそらさないようにする」— 視界に入れば逆に妨害になる。「騒音源との間に立って遮る」の意図と推測。

### F-4. 同一選択肢で数値が矛盾

`c_rival_approach` choice2「何ヤードでした？」に対する相手の答えが、good では「85ヤード。52度で軽めに打ったよ。」、neutral では「…65くらいかな。」。実際に打った距離は一つしかないため、rankで距離が変わるのは不自然。

---

## G. 軽微

- `events.ts` 末尾の統計コメントが stale（「154本」vs 実測164、内訳合計も164）
- `characterEvents.ts` コメント「各キャラ5本×21＝105本」→ 実測107本（キャラ1・2が6本）
- `lunchEvents.ts` コメント「各キャラ1本×16＝16本」→ 実測21本
- `aceEvents.ts` のID連番が飛んでいる（ace_05 → ace_07、ace_06 は private カテゴリの位置に配置）— B-1により未使用
- `extreme_confession` の4番目「その情報を他の人脈に流そうと考える」は行動でなく思考。他の選択肢は全て行動
- `locker_room`: 相手の「最近調子どう？」に対し3択とも質問返し／褒めで、質問に答えていない
- `caddy_ball_advice`（旧NG-3）: 3択とも「渡す」の段階違いで未修正
- `extreme_hole_in_one` `extreme_celebrity_encounter`（stage 5）、`extreme_post_round_offer`（stage 9）はB-3により到達不能／条件付き

---

## 優先順位の提案

### 第1階層：見えている壊れ（テキストを書き足さずに直る）

1. **A-1** 名前差し込み関数を1つ作り、`○○` と `相手` を実行時にキャラ名へ置換する（50＋274箇所が一括で直る）
2. **A-2/A-3** `fullName` を13人分書き直す（結果画面の表示）
3. **A-4** キャラ15・21の呼称をどちらかに統一
4. **E-1** `puttingEvent.ts` の誤アドバイスを傾斜ごとに分岐（20/21キャラの読み合いが復活）

### 第2階層：書いたものを使えるようにする

5. **B-3/B-4/B-5** stage 1・5・9 の扱いを決める。選択肢は（a）stage を「出やすさの重み」に変えて固定ホールと分離、（b）stage 1/5/9 の45件を他stageへ振り直す、（c）ホール数を増やす。**(a) が既存データを1件も捨てずに済む**
6. **B-1/B-2** `aceEvents.ts` `lunchMenu.ts` を削除するか、使う形に接続する
7. **B-6** goodBack/tensionBack の `stage` を実装に合わせる（使うか消すか）

### 第3階層：キャラを立てる（執筆が必要）

8. **C-1/C-2** 朝イチと最終パットを19キャラ分書く。**毎ラウンド必ず通る2箇所**なので、投資効率が最も高い
9. **C-3** curated のセリフをキャラ口調に対応させる（`speech` を speechStyle 別に持つ、または curated を speechStyle 上書きしない方式に変更）
10. **C-5** `Choice.speech` を優先度の高いイベントから埋める
11. **C-6** 主人公の一人称を1つに決めて20箇所を統一

### 第4階層：選択の設計

12. **D-1/D-2** 正解位置をシャッフルする（データ順の入れ替えだけで済む。characterEvents 107件が対象）
13. **D-6** 選択肢ラベルから評価的ト書きを外す
14. **D-3/D-5** extreme の文体テンプレを崩す
15. **D-4** femaleEvents の骨格に変化をつける
16. **D-7** 3択が同rankになる15件のdeltaを差別化
17. **E-2** creep の意味を整理する（「場を冷やした」を別ゲージか focus に寄せるか、creep の呼び名を変えるか）
18. **F-1/F-2/F-3** 成立しない行為3件を書き換え、**F-4** の距離矛盾を修正

---

## 改修履歴

### 2026-07-25 第1階層を実施

| 項目 | 対応 |
|---|---|
| A-1 | `Character.callName` を追加（21人）。`src/lib/characterText.ts` に `withCharacterName` / `localizeEvent` を新設し、`engine.selectEvent` を「抽選（`pickEvent`）→ 名前差し込み」の2段に分離。`getPuttReactionText` にも適用 |
| A-2 | `fullName` の「恒一」13人を個別の氏名に差し替え。`name` に「恒一」を含んでいた大門・金城・白石・鷹宮の4人も修正 |
| A-3 | 銀座ハジメの `fullName` を「弁護士法人エース 代表弁護士」→「銀座 ハジメ」 |
| A-4 | 千鶴の `reactionLines` を藤原→千鶴、ミツキの `reactionLines` とイベント本文を立花→ミツキに統一。`char_16_3` の「エースさん」→「銀座さん」 |
| E-1 | `puttingEvent.ts` に `WRONG_AIM`（傾斜別の誤答方向）を定義し、鬼塚プロフィールと汎用プロフィールの誤アドバイスを傾斜ごとに分岐 |

**検証結果**
- `tsc --noEmit` 通過
- 全21キャラ × 25,200回の抽選で、出力テキストに `○○` / `相手` の残留 **0件**
- 元イベントデータは非破壊（`localizeEvent` はコピーを返す）
- 誤アドバイスが正解と一致するケース: 21キャラ × 4傾斜 = 84通りで **0件**（修正前は20キャラ × 傾斜left で発生）
- `fullName` / `callName` の重複 0件

**副産物**: ホール1の朝イチショットが、固有データを持たない19キャラでも「巌のボールが大きく曲がり、OB方向へ。」とキャラ名で表示されるようになった（C-1の一部緩和。ただし文章自体は共通のまま）。

**この改修で判明した追加事項**
- `engine.getReactionText` はどの画面からも呼ばれていないデッドコード。`character.name`（「銀行マン・田中」）をそのまま文中に埋めていたため、使えば「銀行マン・田中の目が鋭くなった」と表示される状態だった。今回 `callName` を使うよう直したが、使用箇所がないため削除も検討可
- `aceConsults.ts` の「相手」2箇所は、プレイヤーの悩みに対するACEの助言（職場の人物を指す）なので差し込み対象外。ACEラウンドは `selectAceEvent` を通り `selectEvent` を経由しないため、意図せず置換されることはない
- `lunchMiniGame.ts` のコメント見出しに旧呼称（藤原・立花 美月）が残っている（コメントのみ・表示されない）

### 2026-07-25 氏名・呼称ルールの確定

暫定で入れていた氏名13人分は、そのまま正式設定として確定（変更なし）。あわせて id16 の構造と呼称ルールが確定した。

**id16 銀座ハジメの構造**

| フィールド | 値 |
|---|---|
| `name` | `銀座 ハジメ` |
| `fullName` | `銀座 ハジメ` |
| `callName` | `ハジメ` |
| `title` | `弁護士法人エース 代表弁護士`（新設フィールド） |
| `isAce` | `true`（特別キャラクターとして維持） |

肩書きは氏名ではないため `fullName` には入れず、`Character.title`（optional）を新設してそちらへ移した。`ProfileScreen` が `title` を表示し、`fullName` は `name` と異なるときだけ表示するよう変更。

**呼称ルール（全キャラ共通の運用）**

| 場面 | 表記 | 実装 |
|---|---|---|
| 通常イベントの呼びかけ | 「ハジメさん」 | データ側は `○○さん` と書き、`callName` を差し込む |
| 地の文・ナレーション | 「ハジメ」 | データ側は `相手` と書く（キャラ固有イベントは直接 `callName` を記述） |
| プロフィール・正式紹介 | 「銀座 ハジメ」 | `name` / `fullName` |
| 肩書き | 「弁護士法人エース 代表弁護士」 | `title` |
| 特別演出・モード名 | 「ACE」「エース弁護士」 | ACEラウンド／ACEボール等の演出側のみ。一般イベント本文では使わない |

同じ扱いで、千鶴は本文「千鶴」／プロフィール「藤原 千鶴」、ミツキは本文「ミツキ」／プロフィール「立花 美月」。

**このルールに合わせた追加修正**
- id16 の `reactionLines` 10行の「エース」→「ハジメ」（地の文）
- `char_16_1` `char_16_3` `char_16_4` `char_16_5` の状況文4件の「エース」→「ハジメ」
- `char_16_3` の選択肢「銀座さんの過去、興味あります！」→「ハジメさんの過去、興味あります！」
- `characterEvents.ts` / `lunchEvents.ts` のコメント見出しを実名に更新（`lunchEvents.ts` には旧設定の「正義」という名前が残っていた）

**検証**
- `tsc --noEmit` 通過
- 全21キャラ × 16,800回の抽選で `○○` / `相手` の残留 0件、id16 の本文に「エース」「銀座さん」の混入 0件
- `fullName` / `callName` の重複 0件

**`title` の表示先（確定）**
- `ProfileScreen`: 氏名の下に表示
- `CharacterSelectScreen`: 解放済みカードの氏名の下に小さく表示（`title` を持つキャラのみ1行増える）

いずれも `character.title` が未設定のキャラでは何も描画しないため、他20人のレイアウトは変わらない。

**未対応**
- `lunchMiniGame.ts` のコメント見出しに旧呼称（藤原・立花 美月）が残っている（コメントのみ・非表示）

### 2026-07-25 第2階層：ボーナスビートの導入（B-3 / B-4 / B-5）

**判明した前提の訂正**

到達不能だった45件は配置ミスではなく、**存在しないビート用に書かれたコンテンツ**だった。stage 1＝到着シーン、stage 5＝昼食、stage 9＝ラウンド後で、9ホール構成にはそのいずれのビートも無い。したがって当初案の「stage を出やすさの重みに変える」では救えない（「駐車場で相手と合流」がホール3で発生してしまう）。

**採用した方式：ボーナスビート**

ラウンド開始時に3種から**1つだけ**抽選し、そのビートだけを挿入する。ホールを消費しないので **9ビート → 10ビート**。

| ビート | 挿入位置 | 消費するstage | プール |
|---|---|---|---|
| `pre` | 朝イチのショットの前 | 1 | 16件（駐車場・ロッカー・練習グリーン・スタート待ち等） |
| `lunchTalk` | キャラ別ランチイベントの後 | 5 | 20件（昼食・酒・午後スタート前） |
| `closing` | 最終パットの後 | 9 | 16件（風呂・表彰・プロショップ・別れ・見送り） |

**実装**
- `GameState` に `bonusBeat` / `bonusBeatDone`、`GameEvent` に `beat?` を追加
- `engine.applyChoice`: `event.beat` が付いたイベントはホールを消費しない。`closing` はラウンド最終ビートなのでここで終了する
- `engine.applyChoice`: ホール9を終えるとき `closing` が控えていれば完了を保留し、締めを1ビート挟んでから終了する
- `engine.selectEvent`: `pre` / `closing` の抽選を追加
- `engine.selectLunchTalkEvent`（新規）: ランチはホール5で席選び→メニュー→キャラ別イベントという独自フローを持つため、ホール番号ではなく `GameScreenSimple` の lunch サブフェーズから明示的に呼ぶ
- 相談ラウンド（ACE）は独自フローなので `bonusBeatDone: true` にしてビートを発生させない

**ビートの意味に合わせたイベント再配置（9件）**

プレー中の場面が「スタート前」「ラウンド後」「昼食」の枠に混ざらないよう stage を移した。

| イベント | 移動 | 理由 |
|---|---|---|
| `first_hole_result` `c_praise_shot` `c_your_miss` | 1 → 2 | ショット後・プレー中の場面 |
| `last_putt` `c_sunset_hole` `c_final_putt` | 9 → 8 | プレー中の場面（最終パットのミニゲームと重複） |
| `business_talk` `extreme_celebrity_encounter` | 5 → 6 | カート移動中・コース上の場面 |
| `extreme_hole_in_one` | 5 → 4 | プレー中の場面 |

**旧版イベントの抽選除外（B-5）**

`first_tee_tension` `praise_shot` `parking_greeting` は curated 版（rank別セリフ付き）と description が完全一致する劣化コピーだったため、`engine.SUPERSEDED_EVENT_IDS` で抽選から外した。**データは削除していない**ので、配列から外せば復活する。

**検証（21キャラ × 各200ラウンドのフルシミュレーション）**
- 進行の健全性: ハング 0件 / イベント枯渇 0件
- ビート数: 615/630ラウンドが10ビート（残りは creep 爆発による早期終了）
- ビート抽選の偏り: pre 220 / lunchTalk 208 / closing 202（ほぼ均等）
- 発生位置: `pre` は必ず先頭、`lunchTalk` は必ずランチ直後、`closing` は必ず末尾
- **共通プール187件の到達率 100%（未到達 0件）** — 従来は45件が到達不能だった
- バランス影響: 平均 entertainScore 69.5 → 70.8（+1.3）、S率 22.7% → 25.9%、**契約成功率 38.5% → 37.7%（実質変化なし）**

契約判定は動いていないため、閾値の再調整は不要と判断した。S率が3pt上がるのを戻したい場合は `calcGrade` の S 閾値（85）を上げる。

### 2026-07-25 第2階層の残り（B-6 / B-1 / B-2）

**B-6 好調時・不調時プールの `stage` を機能させた**

これまで後半のmood分岐はプールから無条件ランダムで引いていたため、`stage` に書かれた進行順が無視され、「また来月もやりましょうよ！」（`good_back_next_time`）がホール6で出ることがあった。

`pickFromMoodPool` を追加し、共通プールと同じ「同ホール優先・尽きたらプール全体」の方式に統一。あわせて後半の抽選対象ホールが6・7・8であることに合わせ、stage 9 だった4件を stage 8 に移した（`good_back_next_time` `good_back_sunset` `tension_back_score_doubt` `tension_back_cold_call`）。

検証（ホール6/7/8 × 各6,000回抽選）: goodBack 7件・tensionBack 7件すべてが**意図したホールでのみ出現し、未到達0件**。

**B-1 `aceEvents.ts`（20件・296行）— 未使用のまま保持**

`aceConsults.ts`（悩み42件）が上位版として稼働しており、悩みの題材20件はすべてそちらに引き継がれ、回答文もより練られたものに差し替わっていた。つまり本ファイルは草稿。

削除はせず、**先頭に未使用である旨と経緯を明記した**。enjoy-golf はまだ git 管理下に入っていないため（`おもちゃ箱` リポジトリ内で untracked）、消すと復元手段がない。消す場合は下記1行で足りる。

```
rm src/data/aceEvents.ts
```

**B-2 `lunchMenu.ts`（22品）— 未使用のまま保持**

調べたところ単なる重複ではなく、**使われていない上位版**だった。稼働中の昼食ミニゲームは `lunchMiniGame.ts` の `menuItems`（6品・`group` で hearty/japanese/light を判定）を使っており、`lunchMenuPool` は22品＋`isHeavy`/`isLight`/`isLocalSignature`/`priceBand` を持つ詳細版。型 `LunchMenuItem` も本ファイル専用。

メニューの選択肢を6品→22品に増やしたい場合は、`lunchMiniGame.ts` の判定を `group` から本ファイルのフラグへ移せば流用できる。こちらも削除せず、先頭に経緯を明記した。

**第2階層まとめ**

| 項目 | 対応 |
|---|---|
| B-3 stage 1/5/9 の45件が到達不能 | ボーナスビート導入で解消（到達率100%） |
| B-4 curated 16件中6件が死蔵 | 同上（stage 1 の4件は pre ビート、stage 9 の2件は stage 8 へ移して通常枠へ） |
| B-5 events.ts の劣化コピー3件 | 抽選から除外（データは保持） |
| B-6 goodBack/tensionBack の stage 未参照 | 同ホール優先の抽選に変更＋stage 9 の4件を 8 へ |
| B-1 aceEvents.ts | 未使用と判定。経緯をファイル先頭に明記し保持（削除は要判断） |
| B-2 lunchMenu.ts | 未使用の上位版と判定。経緯を明記し保持（流用手順もコメントに記載） |

**推奨事項**: enjoy-golf は23,000行規模だが1コミットも入っていない（`おもちゃ箱` リポジトリ内で untracked）。今後データを削る作業をするなら、先に git 管理下に入れておくのが安全。
→ 2026-07-25 に `add-enjoy-golf` ブランチでコミットし、main へマージ・push 済み（コミット `620f8d9`）。

### 2026-07-25 第3階層 その1：朝イチのショットと最終パットを19キャラ分執筆（C-1 / C-2）

ホール1（朝イチのショット）とホール9（最終パット）は全ラウンドで必ず通るが、固有データがあるのは田中・鬼塚の2キャラだけで、残り19キャラは汎用文にフォールバックしていた。ラウンドの入りと締めが19人とも同じ文章になっていたため、全員分を書き起こした。

**朝イチのショット（`morningShotEvents.ts`）**

19キャラ × 3状況（great / normal / ob）× 3択 = **171選択肢**を追加。執筆ルールをファイル冒頭に明記した。

- 地の文は `callName` で呼ぶ（「相手」と書かない）
- 相手のセリフは `speechStyle` の一人称・語尾に合わせる（巌「わし」、金城の関西弁、スミスの英語混じり等）
- 外し選択肢はそのキャラの `hatesTags` を踏む形で作る。`characters.ts` の `reactions` と likes/hates が加算されるので、同じ言葉でも相手によって結果が変わる
- 選択肢ラベルにナレーターの評価を混ぜない（D-6 の再発防止）

実際の差の例（OB時）:

| キャラ | 「ドンマイです！次いきましょう！」系 | 静かに受ける系 |
|---|---|---|
| 巌（bold/hype を嫌う） | trust −13 | trust +9 |
| 千鶴（bold/over_praise を嫌う） | trust −1 | trust +15 |
| 桐生（flattery/over_support を嫌う） | trust −3 | trust +16 |

**最終パット（`puttingEvent.ts`）**

19キャラ分のプロフィール（`reactionIn` / `reactionLipOut` / `reactionMiss` ＋ 狙い所の言い回し3種）を追加。

アドバイス文は `buildAdvice()` で生成する形にした。キャラごとに「左／まっすぐ／右」の言い回しを1組与えるだけで正解・誤アドバイスを出し分けるので、**E-1 で修正した「誤アドバイスが正解と一致する」バグが構造的に発生しない**。

`adviceAccuracy`（助言の的中率）はキャラの腕前と性格から設定し、読み合いの相手として信用できるかがキャラごとに変わるようにした。黒田 0.9・鷹宮 0.85 に対し、松本 0.3・坊っちゃん 0.35・星野 0.35。

**自分が指摘した問題の再発を1件修正**

初稿では最良選択肢の位置が **A に71%** 偏っていた（D-1 と同じ構造）。実際の適用 delta を計算して最良位置を A→B→C で循環させるよう並べ替え、**A:38% / B:32% / C:30%** にした。

**検証**
- `tsc --noEmit` 通過
- 汎用フォールバックに落ちるキャラ: 朝イチ 0人 / 最終パット 0人（従来は各19人）
- 誤アドバイスが正解と一致するケース: 21キャラ × 4傾斜 = 84通りすべて食い違う
- 地の文・セリフに「相手」の残留: 0件
- 3択すべてが同値になる状況: 0/63
- 既存の田中・鬼塚のデータは JSON 比較で差分なし（追記方式のため無変更）

### 2026-07-25 第3階層 その2：curated セリフのキャラ崩れを解消（C-3）

curated 16件の `choice.speech` は `speechStyle` を上書きするため、192行のセリフが21キャラ全員に同じ文章で出ていた。しかも半数がカジュアル男性口調（〜だね／〜だよ／〜くれ／ハハハ）だったため、千鶴・巌・スミスまで同じ喋り方になっていた。

**方針**

相手は接待される客なので、共通の下限として**丁寧語**に寄せる。ただし口調そのものがキャラの正体になっている相手には共通セリフを使わせない。

**1. 192行を丁寧語ベースへ改稿（132箇所を置換）**

意味と強度は変えず、語尾のみ調整した。

| 旧 | 新 |
|---|---|
| ハハハ！それ聞いたら気が楽になった！ | それを聞いたら気が楽になりました。 |
| 評論家みたいなのはやめてくれ。 | 評論家のようなのはやめてください。 |
| ありがとう、ちょうどいい距離感だね。 | ちょうどいい距離感です。ありがとうございます。 |
| 集中してるんだ、黙っててくれ。 | 集中してるので、黙っていてください。 |

**F-4 の距離矛盾もここで解消**。`c_rival_approach` choice B は good が「85ヤード」、neutral が「65くらい」で同じ一打の距離が食い違っていたため、両方 85 に揃えた。

**2. 口調が正体のキャラは共通セリフを使わない**

`SpeechStyle` に `distinctVoice?: boolean` を新設し、該当スタイルでは共通セリフを捨ててそのキャラ専用の `speechLineTemplates` に戻す。判定は `insight.resolveChoiceSpeech()` に集約した。

| スタイル | キャラ | 理由 |
|---|---|---|
| S01 熱血体育会 | 鬼塚 | 一人称「俺」・命令形混じり |
| S05 外資合理英語混じり | スミス | 一人称「I」・英語混じり |
| S07 昭和礼節重鎮 | 巌 | 一人称「わし」・説教調 |
| S12 女将おもてなし | 千鶴 | 京都の言葉・間合い |
| S17 強気関西不動産 | 金城 | 一人称「ワシ」・関西弁 |

結果、16キャラは選択肢ごとの質の高いセリフを受け取り、5キャラは自分の口調を保つ。トレードオフ（5キャラは選択肢固有の反応を受け取れない）は残るが、キャラ崩れよりは軽いと判断した。将来 `distinctVoice` のキャラ向けに口調別の共通セリフを書けば、そのまま解消できる。

**検証**
- `tsc --noEmit` 通過
- 非セリフ項目（id / stage / title / description / choice text / delta / tags）は JSON 比較で**差分なし**＝セリフ以外を壊していない
- カジュアル語尾の残留 0件（丁寧語の「〜でしょう。」1件を誤検出しただけ）
- 共通セリフを使わないキャラ: 5人（鬼塚・スミス・巌・金城・千鶴）

### 2026-07-25 第3階層 その3：主人公の一人称を統一（C-6）

主人公の一人称が場面によって自分／僕／俺／私に散っていた。選択肢テキスト（＝主人公の発言）だけを対象に走査し、**15箇所を「自分」に統一**した。

「自分」を採用した理由: 既に78箇所で使われて優勢であり、性別中立で、接待の場での謙譲の register に合う。

| 旧 | 新 | 場所 |
|---|---|---|
| 「あのボール、俺が念力で引き寄せます！」 | 「あのボール、自分が念力で…」 | events.ts `pond_dive` |
| 「俺も池に入れたことありますよ（笑）」 | 「自分も池に入れたことありますよ（笑）」 | curatedEvents.ts `c_water_hazard` |
| 「ここは私が」 | 「ここは自分が」 | tensionBackEvents.ts |
| 「僕がいつでも相談に乗りますよ！」 | 「自分がいつでも相談に乗りますよ！」 | femaleEvents.ts（他6件） |
| 「私も緊張していますよ！」 | 「自分も緊張していますよ！」 | morningShotEvents.ts（田中） |

相手のセリフ側（`speechStyles` の「俺」「わし」「ワシ」「I」、`speechLineTemplates`、`characters.reactionLines`）は**キャラの個性なので変更していない**。走査対象を choice の `text` に限定したのはそのため。

未使用の `aceEvents.ts` に残る「僕」4箇所は、ファイル自体が未使用のため対象外とした。

**検証**: 走査を再実行して主人公の一人称の混在 0件。`tsc --noEmit` 通過。

### 第3階層の残り

- C-5 `Choice.speech` の拡充（1,112選択肢が汎用テンプレのまま）。全部書くと数千行になるので範囲を決めてから
- `distinctVoice` の5キャラ向けに口調別の curated セリフを書く（16件 × 3択 × 4ランク × 5口調＝960行）

### 2026-07-25 第4階層 その1：正解選択肢の位置の偏りを解消（D-1 / D-2）

最良の選択肢が1番目に集中していたため、本文を読まず位置だけで正解を選べてしまっていた（キャラ固有イベント98%、ランチ95%、不調時後半84%）。

**採った方法: 表示時のシャッフル**

データを1件ずつ並べ替える方法もあるが、それでは今後追加するイベントで同じ偏りが再発する。`selectEvent` が返す時点で選択肢を並べ替える方式にした。

```
selectEvent = 抽選 → キャラ名の差し込み → 選択肢のシャッフル
```

イベントは `selectEvent` が返したオブジェクトを画面がそのまま保持し、`applyChoice` にも同じオブジェクトを渡すため、index の対応はビート内で一貫する。

**index が意味を持つ箇所は除外した**

| 対象 | 扱い | 理由 |
|---|---|---|
| 最終パット（`putting_event_*`） | **除外** | 選択肢の index が狙い（左／カップ直接／右）に対応しており、並べ替えるとミニゲームの判定が壊れる |
| ACE相談ラウンド | 対象外 | `aceEngine` が独自に3件を抽選して `currentEventConsults` と index を対応させている（既にランダム順で、全 delta 0 なので偏りの問題もない） |
| コンペイベント | **対応** | `CompetitionScreen` が `competitionEvents` を直接読むため、イベント切り替え時の `useMemo` 内で並べ替え（毎レンダリングで呼ぶと並びが踊るので memo 点に置くこと） |

**結果**

| | 変更前 | 変更後 |
|---|---|---|
| 最良の位置 | キャラ固有 1番目98% / ランチ 1番目95% / 女性専用 2番目99% | 1番目 35.1% / 2番目 31.4% / 3番目 28.9% / 4番目 4.6% |
| 最悪の位置 | キャラ固有 3番目93% | 1番目 33.2% / 2番目 32.3% / 3番目 30.3% / 4番目 4.2% |

（4番目の比率が低いのは4択イベントが44件しかないため）

**検証**（21キャラ × 前半・後半 × 各300回 = 12,600回の抽選）
- 位置の偏りが解消（上表）
- 最終パットは「左→カップを直接狙う→右」の順を維持
- 通しシミュレーション: ハング0・イベント枯渇0・共通プール到達率 187/187
- `tsc --noEmit` 通過

**残る副作用**: 選択肢に意図した順序（控えめ→普通→過剰のような段階）を持たせているイベントでは、その並びが失われる。順序を保ちたいイベントが出てきた場合は、`ORDER_LOCKED_PREFIXES` と同じ考え方でイベント側にフラグを持たせれば除外できる。

### 2026-07-25 第4階層 その2：極端な選択肢を「相手次第で正解」にする（D-3 / D-6）

極端選択肢（`extreme` タグ52件）は **21キャラ全員で worst** だった。生 delta が trust −2〜−5／creep 12〜18 と重く、キャラの好みが効く余地がなかった。結果、「！！」の有無だけで地雷を判別でき、選択肢を読む必要がなかった。

**方針**: 派手な選択肢を安全にするのではなく、**派手さという手掛かりを信用できなくする**。判定を生 delta からタグ（＝キャラの好み）へ移す。

**1. 既存31件のリバランス**

型ごとに delta を穏やかにし、タグを文面の実質に合わせた。

| 型 | 件数 | delta | タグ | 刺さる相手 |
|---|---|---|---|---|
| 絶賛（褒め殺し） | 10 | fun7 / trust+4 / creep3 | `humor` `over_praise` | 松本 |
| おだて | 7 | fun6 / trust+5 / creep4 | `flattery` | 巌・千鶴 |
| ノリ（煽る） | 4 | fun7 / trust+4 / creep3 | `humor` `hype` | 光山・松本・星野・篠原・ミツキ |
| 攻め | 1 | fun7 / trust+3 / creep3 | `bold` `risk` `kiai` | 鬼塚・金城 |
| 酒 | 2 | fun5 / trust+2 / creep2 / focus−6 | `alcohol` `bold` | 鬼塚・金城 |
| 世話 | 7 | fun6 / trust−2 / creep7 | `over_support` | **なし（creepのみ緩和）** |
| 不正 | 4 | **変更なし** | `cheat_*` | なし |

**罰は消えていない。移っただけ**。節度重視のキャラには `hatesTags`（creep+6 / trust−4）と `reactions` が乗り、creep +9〜16 に達する。

**プレイヤーの発言文は一切変更していない。** 「今すぐ銘板を発注します！！！」「御神体として祀らせてください！！！」も残した。刺さる相手ではその振り切り方が正解になった時のご褒美になるため。

**ランチ倍率の考慮**: stage 5 は `applyChoice` で delta が1.5倍される。他stageと同じ値では節度重視のキャラまで good に届いてしまったため、stage 5 の4件（`lunch_menu_extreme` `whiskey_offer` `beer_second_round` `afternoon_pep_talk`）は trust を +2 に下げた。

**2. ラベルのナレーター評価を除去（D-6・12箇所）**

極端が正解になり得る以上、評価の先出しは嘘になる。

| 旧 | 新 |
|---|---|
| …と訳のわからない理屈で庇う | …とコースをかばう |
| …とバレる嘘をつく | …と申し出る |
| …と根拠なく断言する | …と言い切る |
| …と勝手に決める | …とOKを出す |
| …と依存心全開で叫ぶ | …と勢いよく言う |
| …とおかしなことを叫ぶ | …と声を張り上げる |

結果や質を先に決める表現（訳のわからない／根拠なく／勝手に／バレる／依存心／おかしな／必死に／強引に／大げさに／押しつける）だけを対象とし、**行為そのものの描写（ごまかす／強がる／見栄を張る）は残した**。

**3. 極端が正解になる局面を6件追加**

3択が「無難」「論理」「極端」の3系統に分かれ、どれが正解かは相手で変わる。無難は誰にも good にならず、攻め好き（鬼塚・金城）には逆に worst になる。

`tee_stage_cheer` `wide_hole_carry` `finish_pose_praise` `veteran_hands` `back_nine_switch` `tucked_pin_dare`

初稿では中間の選択肢を `honesty` にしたところ14人に good が出て新たな支配戦略になったため、`logic` 単独に絞って篠原・早瀬ら数人に限定した。

**検証**

| 項目 | 結果 |
|---|---|
| 極端52件のうち誰かに刺さる | 29件（残り23件は不正・世話・倫理系＝意図通り） |
| 派手な選択肢（「！！」or45字）の21キャラ横断評価 | **最悪56% / 最良16%**（変更前は同条件で77%が最悪） |
| 節度重視12人への漏れ | なし（千鶴は `flattery` に正反応を持つため「おだて」が刺さる側に再分類） |
| creep爆発率 | 2.6%（爆発の仕組みは維持されている） |
| D-1 位置分布 | 最良 36/32/27/5%・最悪 36/31/29/4%（維持） |
| 共通プール到達率 | 193件中 未到達 0件 |
| ハング / イベント枯渇 | 0 / 0 |
| `tsc --noEmit` | 通過 |

**バランスへの影響（要判断）**: ランダムプレイでの平均 entertainScore 69.0 → 71.2、契約成功率 37.7% → 41.5%（+3.8pt）。極端選択肢が全滅ではなくなった分、当てずっぽうでも当たることが増えたため。難易度を戻すなら `calcResult` の `adjustedTrust >= 70`（[engine.ts](src/logic/engine.ts)）を上げるのが一番素直。

### 2026-07-25 難易度の設計：タグ減衰と trust の自然減

「極端が相手次第で正解」を入れた結果、**刺さる型を掴んだら同じ手を押し続けるのが最適**になる懸念があった。その対策としてタグ反応の減衰を実装したが、測ってみると難易度のツマミではなかった。原因を追ったところ別の構造問題に行き着いた。

**1. タグ反応の減衰（実装済み・保険として維持）**

同じタグを1ラウンド内で繰り返すと、そのタグに対する相手の「喜び」が鈍る（1回目100% → 2回目70% → 3回目45% → 4回目以降25%）。

- 減衰するのは `reactions` の**正の反応**と `likesTags` ボーナスだけ
- `choice.delta`（行為そのものの効果）は減衰しない
- `hatesTags` の罰と負の反応は減衰しない（嫌なことを繰り返されて慣れる相手はいない）
- `tagHistory` はラウンド単位でリセットされるので効果もラウンド単位

**実測では効果が小さかった**（最良選択プレイの score 96.8 → 96.0、契約成功率 99.9% → 99.8%）。理由は、熟練プレイは同じタグを連打しないため。イベントごとに最良を選ぶとタグは自然に散る。想定した「同じ手を押し続ける」打ち方自体が発生しない。退化した戦略への保険としては意味があるので残した。

**2. 難易度が上がらなかった本当の理由**

最良選択プレイでは **trust が3〜4ビート目で閾値70に到達する**（74%のラウンド）。

| 到達ビート | 割合 |
|---|---|
| 2ビート目 | 11% |
| 3ビート目 | 43% |
| 4ビート目 | 31% |
| 5ビート目 | 13% |

10ビートのうち最初の3〜4手で契約条件が確定し、残りは結果に影響しない。trust は上限100に張り付くので、閾値を92まで上げても熟練プレイは100%のままだった。

**3. trust の自然減（採用した対策）**

`Character.trustDrift` を新設。1ビートごとに trust が目減りするため、ラウンド全体で稼ぎ続ける必要がある。

| drift | キャラ |
|---|---|
| 2（序盤・ノリで許す） | 田中・鬼塚・坊っちゃん・光山・松本・星野・ミツキ |
| 3（標準） | スミス・巌・中村・金城・千鶴・篠原 |
| 4（手強い） | 黒田・佐藤・大門・白石・桐生・早瀬 |
| 5（最難） | 鷹宮 |
| 0 | ハジメ（相談ラウンドはゲージを使わない設計） |

**落とし穴**: 目減りをそのまま入れると、画面がゲージの前後差から反応ランクを算出しているため毎回 trust −3 が混ざってランクが下振れする。`GameState.lastAppliedDelta`（目減り前の反応そのもの）を `applyChoice` が記録し、ランク判定はそれを使うようにした（`GameScreenSimple` / 旧 `GameScreen` の両方を差し替え）。

**難易度カーブ（実測）**

| 最良を選べる確率 | 変更前 | 変更後 |
|---|---|---|
| 100%（理論値） | 100% | 97.6% |
| 80% | 95% | 83.8% |
| 60% | 85% | 62.3% |
| 40% | 71% | 42.9% |
| 0%（ランダム） | 38% | 13.1% |

creep爆発も機能を維持（ランダムプレイで3.4%）。

**キャラ差は drift だけでは決まらない（既知の課題）**

腕前60%での実測では drift 別平均が 2→58% / 3→70% / 4→55% / 5→51% と単調にならない。難易度を支配しているのは drift より「そのキャラの `reactions` に高 trust の受け皿が何個あるか」だった。

- **大門は16%と突出して難しい**。`reactions` が4つしかなく正の反応は `honesty` +3 だけなので、どの選択肢を選んでも trust が伸びない。意図した難しさではなく設計の穴
- 逆に早瀬は drift-4 でも87%（`honesty` +6 / `logic` +5 と受け皿が広い）

`trustDrift` は全体を押し下げるツマミとしては機能するが、キャラ個別の難易度を作るには `reactions` の設計も併せて触る必要がある。大門の `reactions` 拡充は別作業とする。

### 2026-07-25 モバイル表示：長文選択肢が画面外に出る問題

極端な選択肢を活かす方針にした結果、**長文の選択肢が画面に収まらない**問題が表面化した。派手な選択肢を読ませたいのに読めない状態で、設計と衝突していた。

Xcode 未導入（Command Line Tools のみ）で iOS シミュレータが使えないため、`react-native-web` で実寸検証した。フォントや影はネイティブと完全一致しないが、折り返しと収まりの判断には使える。

**見つかった問題**

| 画面 | 症状 |
|---|---|
| 375×667（iPhone SE） | 73字の選択肢が108pt（4行）。3択で340pt必要なところ214ptしかなく **126pt不足** |
| 320×568（小型端末） | **46pt不足**。3択目が完全に画面外 |
| プロフィール画面 | 球筋が折り返す（「ストレ／ート」）。`statsRow` が中央寄せの親の中で内容幅に縮み、1項目70ptしかなかった |

**対応**

1. **ヘッダーと重複していたフェーズ表示を削除**（全画面で68pt回収）。「前半 1/4」が右上のカードと中央のタグで二重に出ていた
2. **足りないときだけ顔を縮小**（scale 2.5 → 1.4、+26pt）。顔は反応を見る表示なので縮めるのは最小限に
3. **プロフィールの `statsRow` を全幅に**し、Avg を固定56px、球筋を13pxに。最長16字（ハジメ）まで1行に収まることを実測で確認

**判定は画面の実寸から計算する**。固定値では端末差に対応できなかった。

```
選択肢に使える高さ = 画面高 − 415
1行の文字数       = (画面幅 − 60) / 13
```

推定を2回外している。記録として残す。

- 1回目: 閾値を252pt固定にした → 高さ568ptの端末で効かず、画面高から算出するよう変更
- 2回目: 1行を23字固定にした → 幅320ptでは20字しか入らず、2行になる選択肢を1行と見積もっていた。実測から係数を導出

最終的に推定と実測の差は8pt以内（安全側）。

**検証結果（3サイズ）**

| 画面 | 顔 | 不足 |
|---|---|---|
| 320×568 | 圧縮（57pt） | 0pt |
| 375×667 | 通常（92pt） | 0pt |
| 430×932 | 通常（92pt） | 0pt |

**既知の課題**: 大画面（430×932）では下半分が空白になる。現行画面は固定px指定が28箇所あり、画面高さに追従しないため要素が上に詰まる。破綻ではないので未対応。対応するなら顔・ゲージ・余白を比率指定に変える作業になる。

### 全体の進捗

| 階層 | 状態 |
|---|---|
| 第1階層（見えている壊れ） | 完了（A-1 / A-2 / A-3 / A-4 / E-1） |
| 第2階層（書いたものを使えるように） | 完了（B-1〜B-6） |
| 第3階層（キャラを立てる） | C-1 / C-2 / C-3 / C-6 完了、C-5 残り |
| 第4階層（選択の設計） | D-1 / D-2 / D-3 / D-6 完了、D-4 / D-5 / D-7 / D-8 / E-2 / F-1〜F-3 残り |

---

*以上*

### 2026-07-25 反応の読み合いと、ミニゲーム／focus の整理

**1. セリフと仕草のミスマッチを「読み合い」として成立させた**

`mismatchRate` によってセリフと仕草が食い違う仕掛けがあったが、プレイヤーに何も伝わらず、ただの文章の破綻に見えていた。3つの問題があった。

- 仕掛けの存在が画面上のどこにも示されていない
- 用意されていた `mismatchSpeechLines` が21キャラ全員未設定で、仕草だけが裏返っていた
- 見抜いても得がない（`InsightOverlay` は1.2秒の演出のみ）

**本音と建前を逆にした。** 従来は「セリフが本物・仕草が偽物」で、態度を読むと騙される作りだった。接待ゲームの直感と逆なので反転させ、**言葉は取り繕えるが本音は表情と仕草に出る**というルールに統一した。

| | 変更前 | 変更後 |
|---|---|---|
| セリフ | 本音 | 建前（正反対のランクの言葉） |
| 仕草 | 偽物（クラス反転） | 本音（真のランク） |
| 表情 | 本音 | 本音（変更なし） |

建前のセリフは書き下ろさず、`MASK_RANK` で**正反対のランクの既存セリフを流用**する。口調が絶対に崩れず、curated イベントでは選択肢ごとの専用セリフがそのまま建前として機能する（good を隠すと「…お世辞はいらないです。」が出る）。`mismatchSpeechLines` は最優先の上書きとして配線してあるので、個別に書きたくなったら書けば効く。

あわせて:
- 隠すターンは**仕草を必ず表示**（従来70%。唯一の手がかりなのに出ないことがあった）
- 隠すターンの仕草を金色・太字にして、言葉と食い違うサインにした
- 見抜き判定を「隠されたターンの**次の一手**で正解を選べたか」に変更。隠されたターン自体はまだ何も見ていないので運でしかなかった
- 3連続で **ACEボール +1**（`store.gainAceBall`、上限2）。人を読める人は失敗を取り返せる、という筋

**2. ミニゲームの結果を engine 経由にした**

朝イチの自分のショットと最終パットの delta が画面側で直接 clamp されており、`traitModifiers` が効かず「誰が相手でもパットインは trust +5」だった。`applyMinigameResult()` を追加して選択肢と同じ補正経路に通した。

```
パットイン（素の値 trust+5）
  佐藤 1.4 → +7  ／ 田中・巌・桐生・鷹宮・早瀬 1.3 → +7
  黒田・スミス・白石 1.2 → +6  ／ 大門・ハジメ 1.0 → +5
  鬼塚・光山・星野 0.8 → +4（その分 fun が増える）／ 松本 0.7 → +4
```

`trustDrift` はここでは引かない。ミニゲームは同じビートの一部で、ホール分の目減りは `applyChoice` 側で引かれているため。

**3. focus ゲージに役割を与えた**

focus は選択肢で頻繁に上下するのに、勝敗に一切絡んでいなかった（`entertainScore` も `contractSuccess` も trust/fun/creep のみ）。4つあるゲージの1つが実質飾りだった。

**ミニゲームの判定窓の広さ**に接続した。接待に振り切って自分のプレーを疎かにすると、朝イチも最終パットも決まらなくなる。

| focus | 倍率 | 朝イチPERFECT幅 | パットPERFECT幅 |
|---|---|---|---|
| 0 | 0.60 | 12% | 6% |
| 50（初期値） | 1.00 | 20% | 10% |
| 100 | 1.40 | 28% | 14% |

判定と目標ゾーンの描画を**同じ数値から生成**しているので、集中力が落ちるとゾーンが目に見えて狭くなる。新しいUIを足さずに伝わる。

**副産物のバグ修正**: パットの目標ゾーンは表示が 40–60%（朝イチと同じ幅）なのに実判定は 45–55% で、**見た目と判定が食い違っていた**。同じ数値から作るようにしたので解消した。

**検証**: `tsc --noEmit` 通過。ブラウザ実機で focus 50 のときゾーンが PERFECT 20% / GOOD 40%（従来と同一）であることを確認。

---

## E-2 creep ゲージの二重の意味（4点目）

creep は「相手に引かれた度合い」を1本で表しているが、引かれ方には3種類ある。終了時の文面はこれを言い分けていなかった。

### 帰属の誤り

`attributeCreep()` はプレータイプ診断用の `TAG_GROUPS` を流用していた。そのため `serious`（honest群）と `logic`（dominant群）がどの判定にも当たらず、既定の「詰めすぎ」に流れ込んでいた。

creep を持つタグを全データで集計した結果:

| タグ | 選択肢数 | creep合計 | 平均 |
|---|---|---|---|
| serious | 116 | **270** | 2.3 |
| logic | 104 | **125** | 1.2 |

この2タグだけで creep 395 分＝冷たさ由来の約3分の1。正論を並べて引かれた結果が「距離を詰めすぎて引かれた」と表示されていた。

診断用の群とは目的が違うので、`CREEP_SOURCE_TAGS` を別に持たせて creep 本来の意味（引かれ方）で切り直した。

- `cheat` : cheat / cheat_score / cheat_physical / snitch
- `distant` : silence / distance / **serious** / **logic** / neutral / safe / safe_play / avoid_risk / excuse / self_suppress
- `close` : 上記以外（creep の既定の意味）

### 「壁を作られたまま終わってしまった」が発生しない問題

帰属を直した結果、この文面が**実質使われない**ことが判明した。冷たい選択だけで通すと creep は 49〜67 で止まり、100 に届かない。

| | 契約成功 | 平均trust | 平均creep | creep爆発 |
|---|---|---|---|---|
| 冷たく徹する | 16.1% | 44 | 35 | **0.1%** |
| 詰めすぎる | 11.7% | 26 | 67 | 33.6% |

冷たいプレーは失敗するが、**trust が積めないという別経路**で失敗していた。creep 爆発の文面としては死んでいた。

**1回ごとの creep を上げるのは却下した。** それだと慎重に距離を取る1手まで罰することになり、「無難」が常に地雷になる。

代わりに**罰を連続に乗せた**。壁は1回では立たない。

```
COLD_WALL_CREEP = [0, 0, 0, 3, 5, 7, 9]   // index = 連続回数
```

3回目から効き始め、6回目以降で頭打ち。好みへのボーナスが繰り返しで鈍る（`tagDecayRate`）のと対になる形。**そのキャラが好むタグは壁に数えない**（黒田の `serious` など）。相手が望む距離感を守っているだけなので連続が途切れる。

### 結果

冷たさで終わる失敗が成立し、かつキャラごとに違う。

| キャラ | 冷たさcreep | 最終creep | 途中終了率 | 嫌う冷タグ |
|---|---|---|---|---|
| 金城 | 82 | 86 | **37%** | safe, silence |
| 松本 | 78 | 83 | **38%** | serious, silence |
| 坊っちゃん | 80 | 86 | **33%** | serious, logic |
| 星野 | 59 | 76 | **27%** | serious |
| 鬼塚 | 70 | 78 | **20%** | safe, silence, avoid_risk |
| 光山 | 70 | 76 | **20%** | serious, silence |
| 黒田 | 9 | 17 | 0% | —（沈黙を好む） |
| 中村・白石 | 9 | 14 | 0% | — |

寡黙なプロ・黒田に無言で付き合うのは壁にならない。褒め殺し王・松本に正論で返し続けると壁になる。全体の途中終了率は 8.4%。

判定文の内訳も意図通りに出るようになった（冷たく徹したとき `tooDistant` 100%、ランダムプレー時 cheat 23% / tooClose 74% / tooDistant 3%）。

### 回帰

| 腕前 | 契約成功 | 平均score | creep爆発 |
|---|---|---|---|
| 100%（正解を引き続ける） | 97.5% | 93.5 | 0.0% |
| 80% | 81.0% | 88.3 | 0.0% |
| 60% | 62.5% | 81.6 | 0.1% |
| 40% | 42.4% | 73.9 | 0.1% |
| 0%（ランダム） | 13.7% | 56.9 | 2.8% |

難易度曲線は維持。普通のプレーは壁の罰を踏まない（3連続で冷たい選択を選ばないため）。共通プール到達 167/167（superseded 3件を除く全件）、ハング 0 / 4200 ラウンド、`tsc --noEmit` 通過。

---

## D-4 femaleEvents 20件が完全に同一骨格

### 実態は監査時の指摘より徹底していた

選択肢の並び順は D-1 のシャッフルで既に崩れていた（`female_*` は order-locked ではない）。残っていたのは**位置ではなく口調の手掛かり**で、これが完全に固定だった。

- 位置1 は `flattery` ×20（20件すべて）
- 位置2 は `honesty` ×20（20件すべて）、素の delta が trust +6〜8 と突出

| 口調 | 千鶴 | 桐生 | 早瀬 | ミツキ |
|---|---|---|---|---|
| 媚び | worst 18 | worst 20 | worst 20 | worst 20 |
| 誠実 | **good 20** | **good 20** | **good 20** | **good 20** |
| 共感 | neutral 20 | neutral 20 | neutral 20 | good 7 |
| ズレ | worst 20 | worst 19 | worst 18 | worst 17 |

**女性4人 × 20件 = 80回の遭遇すべてで「誠実な方」が正解。** 並び順が変わっても文面を読まずに解ける。千鶴（老舗料亭女将）・桐生（マーケター）・早瀬（税理士）・ミツキ（美容経営）という別人4人が、完全に同じ反応を返していた。

### 方針: 誠実は正解のまま、中身を割る

不正系を「常に罰」で据え置いたのと同じ理屈で、誠実＝正解という背骨は残す。ただし**どの誠実さが刺さるかをキャラが決める**ようにして、相手を読む必要を作る。

差はすでに `characters.ts` に埋まっていた。femaleEvents がそれを選択肢として出していなかっただけ。

| 口調 | タグ | 刺さる相手（その根拠） |
|---|---|---|
| 礼儀 | `etiquette` | 千鶴 `etiquette{t6,f2}`（彼女の最強反応） |
| 論理 | `logic` | 早瀬 `logic{t5,f3}` |
| ノリ | `humor` | ミツキ `humor{f6,t3}`（彼女の最強反応） |
| 誠実 | `honesty` | 千鶴・桐生・早瀬。ミツキには届かない |
| 共感 | `ethics` | 無難。誰にも刺さらないが誰も傷つけない |
| 媚び | `flattery` | 千鶴だけ `flattery{f3,t2}` を持つので致命傷にならない |

判定を素の delta からタグへ移すため、素の値は口調ごとにほぼ揃えた（trust 2〜4 帯）。4件の構成をローテーションさせ、**どの口調も4人全員には刺さらない**ようにしてある。

- A型 礼儀/論理/ノリ/媚び … 千鶴・早瀬・ミツキに正解あり
- B型 誠実/礼儀/ノリ/絶賛 … 4人とも正解あり
- C型 誠実/論理/共感/媚び … 千鶴・桐生・早瀬に正解あり
- D型 ノリ/共感/礼儀/踏込 … 千鶴・ミツキに正解あり

situation（20件の会話の切り口）は残し、選択肢80件を全文書き換えた。

### 結果

| | 旧版 | 新版 |
|---|---|---|
| 4人の最良手が全員一致したイベント | **20/20** | **2/20** |
| 2種類以上に割れたイベント | 0/20 | 18/20 |

残る2件は早瀬で誠実と論理がほぼ同点になるケースで、どちらを選んでも good。

### 回帰

| 契約成功率 | 旧版 100% / 80% / 60% / ランダム | 新版 |
|---|---|---|
| 千鶴 | 100 / 95 / 83 / 21% | 100 / 90 / 75 / 14% |
| 桐生 | 100 / 92 / 70 / 16% | 100 / 88 / 68 / 11% |
| 早瀬 | 100 / 97 / 87 / 25% | 100 / 96 / 86 / 24% |
| ミツキ | 100 / 90 / 71 / 13% | 99 / 85 / 64 / 9% |
| 全21キャラ（80%腕前） | 81.5% | 81.8% |

女性キャラだけ少し難しくなり、全体は横ばい。読む相手が増えたぶんの正しい上昇。

女性イベント到達 20/20、共通プール到達 167/167、`○○` の残留 0件 / 4200ラウンド、ハング 0、`tsc --noEmit` 通過。選択肢の最長は30字（コードベース全体の最長は73字）でレイアウトへの影響なし。

---

## D-7 選んでも反応が変わらないイベント

### 監査時の15件はすでに解消していた

監査は「序盤ゲージ・キャラ平均」で測っていた。その後の trustDrift・タグ反応・`lastAppliedDelta` の導入で、**21キャラ全員でランクが割れないイベントは0件**になっている（230件検査）。

ただし「21人中1人でも割れれば合格」は基準が緩い。何人分で平坦かを測り直した。

| 平坦になるキャラ人数 | 修正前 | 修正後 |
|---|---|---|
| 0〜2人 | 158件 | 162件 |
| 3〜5人 | 32件 | 38件 |
| 6〜8人 | 27件 | 27件 |
| 9〜11人 | 6件 | **3件** |
| 12〜14人 | 7件 | **0件** |

### 根本原因はタグの反応カバー率

キャラが反応を持つタグを集計すると、偏りが極端だった。

| タグ | 好意的 | 嫌悪 |
|---|---|---|
| honesty | 16人 | 0人 |
| humor | 12人 | 0人 |
| **flattery** | **9人** | **8人** ← 最も割れる |
| bold | 5 | 2 |
| logic | 5 | 1 |
| ethics | 4 | 0 |
| etiquette | 3 | 1 |
| serious | 2 | 4 |
| **safe / neutral / over_support / distance** | **ほぼ0** | ほぼ0 |

平坦だったイベントは軒並み `safe` / `neutral` / `over_support` / `etiquette` だけで組まれていた。`opponent_ball_mark` は3択とも `etiquette` で、何を選んでも同じになるのは当然だった。3択が別々の反応を持つタグを踏むように組み替えた。

### 測定中に見つけた、より重い問題：逃げ場のない局面

**3択すべてが worst になる組み合わせが18通りあった。**

| イベント | 該当キャラ |
|---|---|
| rough_to_green_drama | 7人 |
| sand_trap_escape / par3_hole_in_one_near / dramatic_birdie | 各3人 |
| approach_lip_out | 2人 |

いずれも「相手がいいショットを打った、称える局面」なのに、選択肢が3つとも媚び系だった。`flattery` / `over_praise` を嫌う黒田（寡黙なプロ）・桐生・早瀬・中村・鷹宮・佐藤・ミツキには、**good どころか bad すらない**。何を選んでも最悪という詰みの局面になっていた。

「人を持ち上げる（媚び）」と「プレーを具体的に評価する（分析的称賛）」を別の register として分け、後者を1つずつ足した。`analysis_praise` + `honesty` で、素の値は fun 3 / trust 3 / focus 3 に揃えてキャラの反応に判定を委ねている。

```
旧: 「さすがです！バンカーも得意なんですね」            flattery
新: 「砂の取り方が薄かったですね。あの厚さは狙って出せるものじゃないです」
                                          analysis_praise + honesty
```

**逃げ場なし 18通り → 0通り。** 称賛5件の最良手も2〜3通りに割れた（分析12〜15人 / 媚び2〜6人 / 極端4人）。`analysis_praise` を含む選択肢の rank 分布は good 58 / neutral 47 / bad 21 / worst 0（6選択肢 × 21キャラ）で、新たな万能解にはなっていない。

`approach_lip_out` だけは21人全員が分析を選ぶが、残り2択が両方 `cheat_score` なので「正直が正解」で意図通り。

### 意図的に残した3件

`wide_hole_carry` / `tucked_pin_dare` / `extreme_bet_proposal` は10人で平坦のまま残した。

一度は極端側を trust 1 / creep 6 に振って平坦を解消したが、**17人が bad 以下**になり「派手＝地雷」に逆戻りした。極端選択肢を「相手次第で正解」にした設計（D-3/D-5）の方が優先度が高い。攻めの是非に反応を持たないキャラが無関心のままなのは仕様として受け入れる。

### 回帰

| 腕前 | 契約成功 | 平均score | creep爆発 |
|---|---|---|---|
| 100% | 97.9% | 93.7 | 0.0% |
| 80% | 82.3% | 88.3 | 0.0% |
| 60% | 60.2% | 81.7 | 0.1% |
| 40% | 43.2% | 75.0 | 0.4% |
| 0% | 12.8% | 56.7 | 2.9% |

前回（97.5 / 81.0 / 62.5 / 42.4 / 13.7）と同水準。共通プール到達 167/167、ハング 0 / 4200 ラウンド、`tsc --noEmit` 通過。

---

## D-5 extreme の文体が固定テンプレ

delta 側（creep 12〜18）は極端リバランスで対処済み。残っていたのは文体。

### events.ts 側は「絶叫」1種類しかなかった

extreme タグ付き58件を並べると、ファイルで完全に二分されていた。

- **events.ts の39件** — 40〜73字、全部が同じ構造

  ```
  「[称賛]！！[もっと大きい称賛]！！[過剰な行動宣言]！！！」と[ト書き]
  ```

  語彙も集中していた: 神/ゴルフの神 5件、伝説 4件、記念碑・銘板・本・雑誌の表紙 5件、「今すぐ」4件、「〜してください（弟子に/祀らせて/呼ばせて）」4件

- **extremeEvents.ts の19件** — 8〜27字、淡々（「黙ってすり替える」「無言でスマホを見始める」「その情報を他の人脈に流そうと考える」）

つまり共通プール側には「冷たい極端」「無言の極端」「打算の極端」が一切なかった。**削るのではなく型を増やす**方針で、delta とタグは1つも変えずに書き換えた。

| 型 | 件数 | 例 |
|---|---|---|
| 低温型（真顔で、静かに） | 5 | 「…すみません、少し黙らせてください。今の一打、言葉にすると軽くなるので」と帽子を取って立ち尽くす |
| 執拗型（質問と確認が止まらない） | 5 | 「左です。左しかない。左に賭けます。もし右に切れたら自分が謝ります。左です」と繰り返す |
| 過剰準備型（もう済ませてある） | 4 | 「全ホール記録してあります。パーオン率、フェアウェイキープ率、平均パット数。製本してお送りします」と資料を出す |
| 短い断言型 | 16 | 「惜しい、じゃないです！！事件です！！」と言い切る |
| 絶叫型（従来） | 11 | 「ここ一番！！○○さんの見せ場です！！ギャラリーが息を止めてます！！」と全力で盛り上げる |

### 途中で手掛かりを強くしてしまった

長文を圧縮した段階で測ると、**45字以上の最悪率が 50% → 68%、「！！」が 45% → 54% に悪化**していた。穏やかな極端（誰かに刺さるもの）を短く直したため、長く騒がしい枠に厳しいものだけが残る選択効果を自分で作っていた。

原因を見ると、長文枠を占めていたのは `over_support`（過剰な世話）と `cheat_score` で、これらは設計上「誰にも刺さらない」ものだった。長さと無関係に短くも書けるので圧縮した。

さらに「！」を落としすぎて 58件中3件（5%）まで減り、絶叫型がほぼ消えた。派手さを削らない方針に反するので、**good になる極端の側に派手さを戻した**。

- ノリを好む5人（光山・松本・星野・篠原・ミツキ）に刺さる極端 → 最も騒がしく書く
- 巌・千鶴に刺さる極端（控えめな持ち上げ・礼を尽くした辞退） → 静かなまま

派手さが「地雷の目印」から「相手の好みの表現」に変わった。

### 結果

| 見た目の手掛かり | 件数 | イベント内最悪率 | good率 |
|---|---|---|---|
| 「！！」を含む | 16 | 45% → **13%** | 7% → **17%** |
| 45字以上 | 9 | 50% → 44% | 6% → 7% |
| 「！」なし | 473 | 29% → 31% | 15% → 14% |

（3択なら33%が偶然の水準。監査時点の「！！」は77%だった）

**「！！」は最悪率が偶然を大きく下回り、good率は平均を上回った。** 派手な選択肢は、平均すると外れより当たりが多い。

決定的な確認として「派手な選択肢は読まずに避ける」戦略を回した。

| 腕前 | 通常プレー | 派手を避ける | 差 |
|---|---|---|---|
| 100% | 98.0% | 97.6% | **-0.4pt** |
| 80% | 82.6% | 81.4% | **-1.2pt** |
| 60% | 61.8% | 61.0% | **-0.8pt** |

見た目で避ける戦略は、どの腕前でも損になった。手掛かりとして死んでいる。

### 回帰

| 腕前 | 契約成功 | 平均score | creep爆発 |
|---|---|---|---|
| 100% | 98.0% | 93.5 | 0.0% |
| 80% | 83.7% | 88.7 | 0.0% |
| 60% | 60.4% | 81.6 | 0.0% |
| 40% | 41.7% | 74.1 | 0.2% |
| 0% | 13.2% | 57.4 | 2.7% |

難易度曲線は維持。共通プール到達 167/167、`○○` 残留 0件、ハング 0 / 4200 ラウンド、`tsc --noEmit` 通過。選択肢の最長は59字（従来73字）。

---

## F. ゴルフとして成立しない行為 ＋ G. 軽微

### F-4 は既に修正済み

`c_rival_approach` の距離は good / neutral とも「85ヤード」に揃っている（curatedEvents の精読時に対応済み）。

### F-1〜F-3 を、物理的に成立する行為に置き換えた

いずれも creep が架空の行為に付いていた。delta とタグは変えていない。

| | 旧 | 新 |
|---|---|---|
| F-1 `flag_attend_precise` | ショットのタイミングでそっと旗を動かしてピンの位置を相手に有利な方向へ示す | 旗竿をわざと傾けて、狙う方向を無言で指し示す |
| F-2 `final_hole_birdie_attempt` | こっそりカップをわずかに有利な位置に調整しようとする | カップの縁を靴先で押さえて、球が落ちやすいようにしておく |
| F-3 `distraction_noise` | 身を張って相手の視界に入り、注意をそらさないようにする | 隣ホールが見えない位置に回り込み、騒ぎを相手の視界から外す |

- F-1: 旗を動かしてもカップは動かない。旗竿を傾けて狙いの方向を示すのは実在する規則違反（プレーの線を示す行為）で、物理的にも成立する
- F-2: カップは地面に埋設されていて動かせない。カップの縁を踏んで球が落ちやすくするのは物理的に可能で、明確に不正
- F-3: 視界に入れば逆に妨害になる。騒ぎを視界から外す動きに直した

`cheat_physical` / `cheat_score` の全58選択肢を一巡したが、これ以外に物理的に成立しないものはなかった。

### G の内容に関わる3件

| 項目 | 対応 |
|---|---|
| `locker_room` の3択が「最近調子どう？」に答えていない | 1つを「最近はドライバーが安定してきました」と答える形にした（タグも `safe` → `honesty`） |
| `caddy_ball_advice` の3択が「渡す」の段階違いだった | 3つ目を「必要なら言ってくるはず」と口を出さない（`distance`）に変え、2つ目を `etiquette` にした |
| `extreme_confession` の4番目が行動でなく思考だった | 「その情報を他の人脈に流そうと考える」→「聞いた話を、あとで使えるようスマホにメモする」 |

`safe`（反応を持つキャラがほぼゼロ）を `honesty` / `etiquette` に振り直しているのは D-7 の処置と同じ理由。

### G の stale コメント

| ファイル | 旧 | 新 |
|---|---|---|
| `events.ts` | 総数: 154本（既存76 + 新規78） | 総数: 170本 |
| `events.ts` | over_support extreme creep 10-15 | creep 6-8（極端リバランス後の実値） |
| `events.ts` | cheat extreme creep 15-20 | creep 5-18 |
| `characterEvents.ts` | 各キャラ5本 × 21 = 105本 | 各キャラ5〜6本 × 21 = 107本 |
| `lunchEvents.ts` | 各キャラ1本 × 16 = 16本 | 各キャラ1本 × 21 = 21本 |

### G のうち解消していたもの

`extreme_hole_in_one` / `extreme_celebrity_encounter` / `extreme_post_round_offer` の到達不能は B-3 で解消済み。全プールの到達率を測り直した。

| プール | 到達 |
|---|---|
| events | 167/170（superseded 3件を除く全件） |
| extremeEvents | 10/10 |
| curatedEvents front / back | 8/8 / 8/8 |
| femaleEvents | 20/20 |
| goodBackEvents / tensionBackEvents | 7/7 / 7/7 |

### 残した項目

- **`aceEvents.ts` は完全に未使用**（B-1 で `aceConsults` に置き換わった）。ID連番の飛びは死んだファイル内の話なので直していない。ファイルごと削除するかは要判断
- **タグの意味が行為と合っていないもの**が3件ある。`extreme_final_hole_allIn`「わざとミスして相手を気持ちよく勝たせる」と `comp_exec_b2`「わざと負ける」は不正タグ（`cheat_physical` / `cheat`）だが、実際は接待の一形態。`char_5_2`「先に打っちゃいましょう」も打順の話で物理的な不正ではない。罰の量としては機能しているため今回は触っていない

### 回帰

| 腕前 | 契約成功 | 平均score | creep爆発 |
|---|---|---|---|
| 100% | 98.3% | 93.6 | 0.0% |
| 80% | 82.4% | 88.1 | 0.0% |
| 60% | 62.9% | 82.4 | 0.1% |
| 40% | 42.4% | 74.5 | 0.6% |
| 0% | 12.9% | 57.2 | 2.5% |

難易度曲線は維持。`○○` 残留 0件、ハング 0 / 5250 ラウンド、`tsc --noEmit` 通過。

### 追補: 不正でない行為に付いていた cheat タグを外した

F の項で「据え置き」と書いた3件を修正した。実測すると、罰の量ではなく**終了文が嘘になる**問題だった。

`CREEP_SOURCE_TAGS.cheat` に `cheat_physical` / `cheat` が入っているため、これらを選んで creep が振り切れると結果画面に「ごまかしを見透かされてしまった...」と出る。わざと負けた人・打ち込んだ人はごまかしていない。E-2 で整理した「引かれ方の言い分け」と矛盾していた。

| 選択肢 | 旧タグ | 新タグ |
|---|---|---|
| `extreme_final_hole_allIn`「わざとミスして相手を気持ちよく勝たせる」 | cheat_physical, extreme, over_support | over_support, flattery, extreme |
| `char_5_2`「先に打っちゃいましょう」（打ち込み） | cheat_physical | bold |
| `comp_exec_b2`「わざと負ける」 | flattery, cheat | flattery |

**罰の量は変わらない。** `extreme_final_hole_allIn` は cheat_physical を外しても worst 21人のまま（`over_support` + creep 10 + trust −3 で足りている）。creep の帰属だけが `{cheat:22}` → `{close:13}` に変わり、終了文が「距離を詰めすぎて引かれてしまった」になる。

`cheat_physical` の枠（1ラウンド2回）の消費も解消したが、そもそも上限に達するのは 6,300ラウンド中 0.1% だったので実害はなかった。

回帰: 98.3 / 83.7 / 62.7 / 42.9 / 13.2%（前回 98.3 / 82.4 / 62.9 / 42.4 / 12.9）、`tsc --noEmit` 通過。

### 到達不能なレガシー画面 3,758行（未対応・要判断）

`'Home'` へ遷移するコードが存在せず、レガシー画面が丸ごと届かない。

| ファイル | 行数 | 現行の対応物 | 差 |
|---|---|---|---|
| `screens/GameScreen.tsx` | 1,635 | `GameScreenSimple.tsx`（2,718行） | 朝イチショット・最終パット・ホールマップが旧版に0件 |
| `screens/HomeScreen.tsx` | 823 | `CharacterSelectScreen.tsx` | リセット機能・契約済み表示が旧版になし |
| `screens/ResultScreen.tsx` | 560 | `ResultScreenSimple.tsx`（351行） | 終了文の出し分けが旧版になし |
| `data/aceEvents.ts` | 308 | `aceConsults.ts`（42件） | 回答文が練り直されている |
| `portraits/index.tsx` | 276 | — | HomeScreen だけが使用 |
| `data/globalState.ts` | 156 | `store/useGameStore.ts` | 死んだ3画面だけが使用 |

いずれも現行画面の作り直し前の版で、新しい機能は含まれていない。今セッションの改修（`diagnoseCreepCause` / `applyMinigameResult` / `focusWindowScale`）はどれも死んだ側に入っていない一方、`insight` / `faces` / `lunchMiniGame` / `types` を import しているため型を合わせ続ける負担だけが残る。

### 到達不能なレガシー画面 3,758行を削除（対応済み）

`'Home'` へ遷移するコードが存在せず、レガシー画面が丸ごと届かない状態だった。3枚とも現行画面の作り直し前の版で、新しい機能は一つも含まれていない。

| 削除したファイル | 行数 | 現行の対応物 |
|---|---|---|
| `screens/GameScreen.tsx` | 1,635 | `GameScreenSimple.tsx`（朝イチショット・最終パット・ホールマップは現行のみ） |
| `screens/HomeScreen.tsx` | 823 | `CharacterSelectScreen.tsx`（リセット・契約済み表示は現行のみ） |
| `screens/ResultScreen.tsx` | 560 | `ResultScreenSimple.tsx`（終了文の出し分けは現行のみ） |
| `data/aceEvents.ts` | 308 | `aceConsults.ts`（回答文を練り直した42件） |
| `portraits/index.tsx` | 276 | HomeScreen だけが使用していた |
| `data/globalState.ts` | 156 | `store/useGameStore.ts` |
| 計 | **3,758** | |

あわせて `App.tsx` から3つの import と3つの `Stack.Screen`、`types/index.ts` の `RootStackParamList` から `Home` / `Game` / `Result` を削除（16行）。

**検証**: `tsc --noEmit` 通過。ブラウザでキャラ選択 → プロフィール → ラウンド開始 → 朝イチのショット → 自分のショット（ミニゲーム）まで実際に通し、コンソールエラーなし（`transform-origin` の DOM プロパティ警告2件は SVG 由来の既存警告で、削除とは無関係）。

契約成功率 97.8 / 82.8 / 61.6 / 43.4 / 13.1%（前回 98.3 / 83.7 / 62.7 / 42.9 / 13.2）、共通プール到達 167/167、ハング 0 / 4200 ラウンド。

---

## 大画面で下が空く問題（固定px積み上げ）

`430×932` で下3割が空白になっていた。固定pxを積み上げて上詰めで並べていたため、画面が高くなっても要素が上に留まっていた。

### 直し方

28箇所の固定値を全部比率にするのではなく、**余った高さの使い道を決める**方針にした。

**1. 高さに応じて顔と余白を広げる**

```ts
// iPhone SE 相当の 667pt を基準に、余った高さの分だけ広げる。
// compactLayout（内容が入りきらない）のときは逆に詰めたいので効かせない。
const roomyScale = compactLayout ? 1 : Math.min(1.4, Math.max(1, windowHeight / 667));
const faceScale = compactLayout ? 1.4 : 2.5 * roomyScale;
const roomyGap = Math.round(16 * roomyScale);
const roomyPad = Math.round(14 * roomyScale);
```

顔・顔まわりの余白・イベントカードのパディング・選択肢のパディングに適用。932pt で顔が 2.5 → 3.5 倍になる。

**2. それでも余る分は選択肢の上下に配る**

`scrollContent` に `flexGrow: 1` を足し、選択肢の前後に `flex: 1` と `flex: 0.45` の空きを入れた。選択肢が下寄りになって親指で届きやすくなり、余りの一部は下にも残るのでボタンが画面の端に貼り付かない。

内容が画面より高いときは `flex` が 0 に潰れるため、**従来どおり上端から並んでスクロールする**（`compactLayout` のときは空き自体を出さない）。

**3. プロフィール画面**

`content` に `flexGrow: 1` はあったが `justifyContent` がなく上詰めだった。`justifyContent: 'center'` を追加。結果画面（`ResultScreenSimple`）は既に中央寄せ済みだった。

### 検証（ブラウザ実機）

| 画面サイズ | 結果 |
|---|---|
| 430×932 | 顔が拡大し、選択肢が下寄りに。**空白 3割 → 1割弱** |
| 375×667 | ちょうど収まる（`roomyScale` = 1.0 で従来と同一） |
| 320×568 | `compactLayout` が働き、従来どおり上詰め＋スクロール（余剰 154pt でスクロール可能を確認） |

プロフィール画面も 430×932 で中央寄せになり、320×568 では上詰め＋スクロール（余剰 109pt）。

`tsc --noEmit` 通過。コンソールエラーなし（`transform-origin` の DOM プロパティ警告は SVG 由来の既存警告で、今回の変更とは無関係）。

---

## 320幅でヘッダーのカードが切れる問題

`compHeader` は `flexDirection: 'row'` ＋ `justifyContent: 'space-between'` だが、氏名側に `flex` も `minWidth: 0` もなかった。Text の内容が最小幅として効くため、氏名が長いキャラでは右のホールマップのカードが画面外へ押し出されていた。

氏名は「役割・名前」形式で、最長は「外資エリート・アレクサンダー・スミス」の18字。fontSize 18 では名前だけで 320幅を使い切る。

### 末尾省略ではなく呼び名に落とす

`numberOfLines={1}` で切り詰めるだけだと「外資エリート・アレクサン…」となり、**本人の名前が消えて誰と回っているか分からなくなる**。役割はプロフィール画面で読めるので、入らないときは役割を落として `callName`（最長5字）にした。

判定は画面幅ではなく「入るかどうか」で行う。画面幅で切ると、375幅では入らない13字の名前が切り詰められてしまう。

```ts
const nameRoom = windowWidth - 32 - (narrowLayout ? 110 : 124) - 8;
const headerName =
  character.name.length * nameFontSize <= nameRoom
    ? character.name
    : character.callName;
```

| 画面幅 | 氏名に使える幅 | 役割込みで表示 |
|---|---|---|
| 320 | 170pt | 14/21人 |
| 375 | 211pt | 16/21人 |
| 390 | 226pt | 17/21人 |
| 430 | 266pt | 20/21人（スミスのみ呼び名） |

### 押し出し自体も塞いだ

呼び名でも収まらない事態が将来起きても崩れないように、縮む側と縮まない側を明示した。

- `compHeaderLeft` に `flex: 1` と `minWidth: 0`（`minWidth` を 0 にしないと Text の内容が最小幅として残る）
- `compHeaderName` に `flexShrink: 1` ＋ `numberOfLines={1}`
- `holeMapBtn` と `aceBallBadge` に `flexShrink: 0`（情報表示なので潰させない）

狭い画面ではホールマップも 56×42 → 46×34、氏名は 18 → 16pt に縮めて氏名の枠を広げた。ヘッダーは3画面（通常イベント・朝イチ・最終パット）にあるので3箇所すべてに適用した。

### 検証（ブラウザ実機）

| 画面 | 結果 |
|---|---|
| 320×568・スミス（18字） | 「スミス」表示。カード全体が見える。`overflowsRight: false` |
| 320×568・坊っちゃん（13字） | 「坊っちゃん」表示。カード全体が見える |
| 375×667・スミス | 「スミス」表示（切り詰めの「アレクサン…」が出なくなった） |
| 430×932・坊っちゃん | 「二代目オーナー・坊っちゃん」フル表示。カードも全体が見える |

`tsc --noEmit` 通過。

---

## コンペラウンドのバグ調査

### 実機で再現した症状

春の名門オープンを trust 100 / rep 100 で勝ち切った直後の保存データ。

| 項目 | 期待 | 実際 |
|---|---|---|
| `competitionCleared` | `{springOpen:true}` | **`{}`** |
| `unlockedCharacterIds` に篠原(17) | 含まれる | **含まれない** |
| `contractedCharacterIds` | `[1]` | **`[1,17]`** |
| `totalContracts` | 1 | **2** |
| 誤って解放されたキャラ | なし | **ミツキ(21)** |

画面には「篠原さんとの通常ラウンドが解放されました！」と出るのに、**篠原は解放されず、代わりにミツキが解放される**。コンペのクリアも記録されないので何度でも繰り返せる。

### 原因1: スナップショットでの全体置き換え

`useGameStore` の `addContractForCharacter` だけが、唯一 `setState((prev) => ...)` ではなく `stateRef.current` のスナップショットで**状態全体を置き換えていた**（他は全て関数形。例外は意図的な全リセットのみ）。

`stateRef.current = state` はレンダー時代入なので、同じ tick で2つ呼ぶと2つ目が1つ目の更新を消す。結果画面は次の順で呼んでいた。

```ts
store.markCompetitionCleared(competitionId);           // setState((prev) => ...) で積まれる
store.addContractForCharacter(result.targetCharacterId); // 古い state で全体を上書き → 上が消える
```

**同じ被害が通常ラウンドにも出ていた。** `ResultScreenSimple` は契約成立時に `incrementRoundCounter()` → `handleRoundComplete(characterId)` → `addContractForCharacter(characterId)` の順で呼ぶため、**契約が取れたラウンドではラウンド数の加算とクールダウンの設定が消えていた**（コンペの解放が遅れ、契約した相手をすぐ再プレーできる）。

更新を関数形に変えた。戻り値（解放演出に渡すリスト）は従来どおり呼び出し時点の値から求めている。

### 原因2: コンペのクリアで契約が付いていた

対象キャラの解放条件は `competitionClear` なので、`markCompetitionCleared` だけで解放は足りる。`addContractForCharacter` は解放に不要な上、1度も回っていない相手が契約済になっていた。

- 契約数が水増しされ、`totalContractsAtLeast: 5` の銀座ハジメが3件ぶん早く解放される
- `contractWith: 17` のミツキ、`contractWithAny: [21,18]` の早瀬が誤って解放される
- 一覧に「契約済」と嘘が出る

`unlockText` が「通常ラウンドが解放されました」と書いている通り、コンペは解放であって契約ではない。呼び出しを削除した。

### 原因3（原因1を直して露出）: 解放メッセージが即座に消える

`isFirstClear` を毎レンダー `store.isCompetitionCleared()` から計算していた。クリアを記録した瞬間に `false` になり、これを表示条件にしている解放メッセージが消える。

**このバグは以前から存在したが、原因1に隠れていた**（クリアが記録されないので `isFirstClear` が `true` のままだった）。同画面の他所と同じく `useRef` で初回値を固定した。

### 修正後（同手順で再実行）

```
competitionCleared: { springOpen: true }
contractedCharacterIds: [1]
totalContracts: 1
unlockedCharacterIds: [1, 2, 3, 17]
画面: 「篠原さんとの通常ラウンドが解放されました！」
一覧: 「春の名門オープン クリア済」／契約 1/21
```

`characterList` は19件で篠原も `status='unlocked'` で含まれる（リスト内17番目）。

> 調査中、一覧の DOM に篠原が出ないため4つ目のバグを疑ったが、`FlatList` の仮想化（`initialNumToRender` 既定10）で10行しか DOM に載らないだけだった。ロジックを Node で再現して19件・篠原ありを確認済み。私の計測側の問題。

### バランス（バグではない）

4イベントの全組み合わせ 3^4 を全探索した結果。難易度の並びは意図通り。

| コンペ | 閾値 | 成功する組み合わせ |
|---|---|---|
| 春の名門オープン（easy） | trust 60 / rep 45 | 59/81（73%） |
| シーサイドチャリティ（medium） | trust 65 / rep 50 | 24/81（30%） |
| 経営者杯（hard） | trust 70 / rep 50 | 27/81（33%） |

### 未修正（軽微・報告のみ）

- `CompetitionGameState.finished` はどこでも `true` にならない死んだフィールド
- `phase` の `'lunch'` は型にあるが代入箇所がない（昼は画面側の `step` で管理しているため）
- コンペ用エンジンには通常ラウンドで入れたタグ減衰（`tagDecayRate`）と creep 爆発による中断がない。4択の短いモードなので現状のままでも成立するが、通常ラウンドと挙動が違う

### 軽微3点の追加対応

**1. 死んだフィールド `finished` を削除**

`CompetitionGameState.finished` は宣言と初期化だけで、`true` にする箇所も読む箇所もどこにもなかった。削除した。

**2. `phase` の `'lunch'` を実際に使うようにした**

型に `'front' | 'lunch' | 'back'` とあるのに `'lunch'` を代入する箇所がなく、前半2つを終えた時点で `'back'` に飛んでいた。前半2つの直後を `'lunch'` にした。

ヘッダーの表示式は `step === 'event'` の中だけで使われるので影響はない。全ステップを通して確認した。

```
開始              phase=front  index=0  → 「前半 イベント 1 / 2」
前半1の後         phase=front  index=1  → 「前半 イベント 2 / 2」
前半2の後（昼へ） phase=lunch  index=2  ※昼はヘッダー非表示
昼の後            phase=back   index=2  → 「後半 イベント 1 / 2」
後半1の後         phase=back   index=3  → 「後半 イベント 2 / 2」
後半2の後（表彰） phase=back   index=4  ※awards へ
```

**3. タグ減衰を通常ラウンドと揃えた。creep 爆発は追加しない**

`tagDecayRate` を移植し、正の反応と `likesTags` のボーナスを繰り返しで鈍らせた（`hatesTags` と負の反応は減衰させない）。通常ラウンドの `engine.ts` と同じ扱い。

creep 爆発による中断は**追加しなかった**。全81通りを探索した結果、creep 100 に届く組み合わせが1つもない。

| コンペ | creep 最大 | creep>85 で失敗 |
|---|---|---|
| 春の名門オープン | 20 | 0/81 |
| シーサイドチャリティ | 97 | 2/81 |
| 経営者杯 | 82 | 0/81 |

仕組みを足しても到達しないコードになるため入れていない。あわせて分かったこととして、`calcCompetitionResult` の `creep <= 85` は春の名門（最大20）と経営者杯（最大82）では常に真で、実質シーサイドチャリティ専用の条件になっている。

**減衰後のバランス**（成功する組み合わせ）

| コンペ | 減衰前 | 減衰後 |
|---|---|---|
| 春の名門オープン | 59/81（73%） | 59/81（73%） |
| シーサイドチャリティ | 24/81（30%） | 22/81（27%） |
| 経営者杯 | 27/81（33%） | 26/81（32%） |

難易度は下がっていない。`tsc --noEmit` 通過。

**判断: 引かれ度の条件は現状のまま据え置く（2026-07-29 確認済み）**

`calcCompetitionResult` の成功条件は「信頼度 ≥ 閾値」「評判 ≥ 閾値」「creep ≤ 85」の3つだが、3つ目は3コンペのうち2つで原理的に満たされてしまう。

| コンペ | creep 最大値（全81通り） | 85を超える組み合わせ |
|---|---|---|
| 春の名門オープン | 20 | 0/81 |
| 経営者杯 | 82 | 0/81 |
| シーサイドチャリティ | 97 | 2/81 |

つまりコンペで負けるときは必ず「信頼度か評判が足りない」で負け、「引かれすぎて嫌われた」という負け方はほぼ存在しない（シーサイドチャリティの2通りのみ）。通常ラウンドの creep 爆発に相当するものがコンペには成立していない。

**壊れてはいないため、仕様として現状のままとする。** コンペは4択の短いモードで、引かれ度で落とす必要はないという判断。効かせたくなった場合は、春の名門と経営者杯の外し選択肢の creep を上げる作業になる（エンジン側の変更は不要）。

---

## エースラウンド（銀座ハジメ・相談モード）のバグ調査

広告の核になる部分なので、エンジン・データ・画面・保存の全経路を確認した。

### 中核の仕組みは堅牢だった

400ラウンド / 2000ホール分を通した結果。

| 検査項目 | 結果 |
|---|---|
| 相談文と回答の対応ズレ | **0件** |
| 回答が空 | 0件 |
| 全ラウンドが5ホールで complete 終了 | OK |
| 同一ラウンド内での相談の重複 | 0件 |
| 相談プール | 42件・ID重複0・名言候補21件 |

`getSelectedAceConsult` がモジュール変数を読む設計だが、抽選は `useState` の遅延初期化と `advanceToNext` の1箇所ずつなので、選んだ相談と返る答えがズレることはない。ACEボールの誤発火も、相談ラウンドが早期 return するため起きない。

### 見つかった問題

**1. 相談内容が結果画面に残らなかった**

> 当初これを「架空の18ホールスコアを出している」と報告したが、**私の誤りだった**。
> 設定上は18ホール回っていて、相談は5つのビートとして起きている。
> スコア表示（18H・ベースライン・接待グレード）は正しいので元に戻した。

実際の課題は、5つの相談で何を言われたかが結果画面に一切残らないことだった。共通レイアウトのスコア表示の下に「今日いただいた言葉」として追加した。

`holeResults` に複合ID（`ac_01+ac_07+ac_15`）と選んだ index が残っているので、追加の状態を持たずに「何を相談して何と言われたか」を復元できる（`getAceRoundConsults`）。300ラウンドで復元ミス0を確認。

助言は相談を小さく、答えを大きく置き、名言は金色で立てている。

**2. 顧問契約の場面に、起きていない昼の話が出ていた（重要）**

`createAceInitialState` が `lunchMood: 'good'` を入れていたため、`IntroScreen` が「昼の時間、良かったですね。」を表示していた。相談ラウンドに昼はない。`'neutral'` にして解消。

**3. 名言が保存されるだけで、どこにも表示されなかった**

`saveQuoteIfNew` が `aceQuotes` に貯めて端末に永続化していたが、読む画面が存在しなかった。ラウンドの入口であるハジメのプロフィールに「もらった言葉 n / 21」として置いた。

**4. 名言の発動率が低すぎた**

`isQuote` に加えて25%の抽選を掛けていた。実測すると名言候補は平均2.4回/5手選ばれるのに、抽選のせいで**実際の発動は0.63回/ラウンド**まで落ち、約半分のラウンドで一度も名言が出なかった。

抽選率ごとの実測（4,000ラウンド）。

| 抽選率 | 発動回数/ラウンド | 出ないラウンド |
|---|---|---|
| 0.25（従来） | 0.63回 | 約50% |
| 0.40 | 0.97回 | 33% |
| **0.50（採用）** | **1.23回** | **24%** |
| 0.60 | 1.45回 | 18% |
| 1.00（抽選なし） | 2.43回 | 3% |

「ラウンドに1回強」を狙って 0.5 にした。

**5. 死んだフラグ・到達しない分岐**

- `aceUsedThisRound` はどこでも `true` にならず、最終パットのゲート `!state.aceUsedThisRound` が機能していなかった。条件から外した
- `calcResult` の `creepThreshold = isAce ? 70 : 85` は、エースが必ず `calcAceResult` を通るため到達しない。85 固定にした

### 未修正（報告のみ）

- `refillAceBalls` は定義・公開されているが呼び出し元がゼロ（ACEボールは初回の顧問契約時と洞察成功時のみ増える）
- エースラウンドはラウンド数の加算とクールダウンの対象外（`ResultScreenSimple` が `!isAceRound` でガード）。何度でも即再訪できる。広告としては妥当とも読めるため据え置き

### 回帰

契約成功率 98.2 / 82.6 / 60.9 / 41.3 / 14.0%（前回 98.0 / 83.7 / 60.4 / 41.7 / 13.2）。共通プール到達 167/167、ハング 0 / 4200、最終パットの到達を確認、`tsc --noEmit` 通過。

---

## ACEボールの挙動チェック

### 正しく動いていたもの

| 検査 | 結果 |
|---|---|
| bad / worst のときだけポップアップが出る | OK |
| 「やり直す」でボールが1個減る（2→1） | OK |
| ヘッダーのバッジが連動（ACE x2 → x1） | OK |
| やり直し後、同じイベントの3択が選び直せる | OK |
| ボールが残っていれば連続でやり直せる | OK |
| 「このまま」では消費せず次へ進む | OK |
| 相談ラウンドでは出ない（早期 return のため） | OK |
| ゲージ・`usedEventIds`・`cheatPhysicalCount` の巻き戻し | OK（`previousState` に入っている） |

### 見つかった問題

**1. ポップアップの文字が読めなかった（重要）**

`aceBallPopup` の背景が `COLORS.cardBg`（8%の白）でほぼ透明だったため、後ろのセリフと仕草が透けて文字が重なっていた。貴重なボールを使うか決める場面で判読できない状態だった。不透明な板（`#14301f`）＋影に変更。

**2. やり直しても、取り消した選択のペナルティが残っていた（重要）**

`updateTanakaState` / `updateOnizukaState` はモジュール変数を加算するだけで、`GameState` に入っていないため `previousState` では戻らない。結果、ACEボールで選択を取り消しても次が残っていた。

- 媚びスタック（`flatterStack >= 3` で fun 倍率 0.7、`>= 5` で気合の要求が上がる）
- **不正カウント（`cheatCount === 0` が S 評価の条件）** — 不正を取り消しても S 条件が壊れたまま
- 洞察の連続数（`insightStreak`）と直前のミスマッチ判定（`prevWasMismatch`）

`snapshotTanakaState` / `restoreTanakaState`（鬼塚も同様）を追加し、選択の直前を控えて「やり直す」で復元するようにした。田中の8項目すべてが戻ることを確認。

```
鬼塚: 媚び2回 → fun倍率 1.0
      媚び3回 → fun倍率 0.7
      やり直し → fun倍率 1.0   ← 戻る
田中: 不正1回 → cheatCount 1
      やり直し → cheatCount 0  ← 戻る（S条件が復活）
```

影響を受けるのは田中と鬼塚だけだが、この2人は最初に出会うキャラクター。

**3. ボール獲得の判定が `setState` の実行タイミングに依存していた**

```ts
const gainedRef = useRef(false);
const gainAceBallFn = useCallback(() => {
  gainedRef.current = false;
  setState((prev) => { ...; gainedRef.current = true; ... });
  return gainedRef.current;   // ← 更新関数がまだ走っていない可能性
}, []);
```

React は `setState` の更新関数を必ず同期実行するわけではない（同じ tick に他の更新が積まれていると遅延する）。その場合ボールは増えるのに `false` が返り、**獲得の演出（`showInsight`）が出ない**。同期的に読める `stateRef` で判定するように書き換えた。

**4. バッジが朝イチ／最終パットの画面だけ消えていた**

ACEボールのバッジは通常イベントと昼の画面にはあるが、朝イチのショットと最終パットの画面には無く、ビートをまたぐたびに現れたり消えたりしていた。両方に足して統一。

### 未修正（報告のみ）

- `refillAceBalls` は定義・公開されているが呼び出し元がゼロ。ボールは初回の顧問契約時（2個）と洞察成功時（上限2個）のみ増える
- ポップアップの「残り: n個」は使用前の数を出している。「やり直す」を押すと n-1 になる。表記としては正しいが、押す前に減った数を見せたい場合は要調整

### 検証

ロジック単体（スナップショット復元の全項目確認）＋実機で鬼塚ラウンドを通し、不正選択 → ポップアップ → やり直す → 同じ3択に戻る → もう一度選ぶ → 「このまま」で前進、までを確認。`tsc --noEmit` 通過。

---

## 全体バグチェック（ここまでの総点検）

### 機械的な検査結果

`tsc --noEmit` 通過。データ整合性（選択肢 1,343件）。

| 検査 | 結果 |
|---|---|
| text が空 / tags が空 / delta 未定義 | 0 / 0 / 0 |
| delta に数値以外・NaN | 0 |
| イベントIDの重複 | 0 |

通しシミュレーション 6,300ラウンド。

| 検査 | 結果 |
|---|---|
| ハング | 0 |
| `○○` の残留 | 0 |
| `undefined` / `NaN` の混入 | 0 |
| 選択肢が2未満のイベント | 0 |
| ゲージが範囲外・NaN | 0 |
| イベントが null | 0 |
| ホールが9を超えた | 0 |
| `finishReason` が想定外 | 0 |

難易度曲線 98.1 / 81.1 / 61.4 / 41.3 / 13.3%。共通プール到達 167/167、他プールは全件到達（extremeEvents 10/10、curated 8+8、female 20/20、goodBack/tensionBack 7/7）。

### 今日入れた修正の不変条件（すべて維持）

| 項目 | 目標 | 現在 |
|---|---|---|
| D-7 逃げ場なし（3択とも worst） | 0 | **0 通り** |
| D-7 平坦 15人以上 | 0 | **0 件**（9人以上は意図的に3件） |
| D-5 「！！」の最悪率 | 33%（偶然）以下 | **13%**（good率 17%） |
| D-4 女性4人の最良手が全員一致 | 少数 | **2/20**（旧 20/20） |
| E-2 冷たく徹したときの途中終了 | 成立させる | **8.2%**（内訳すべて tooDistant） |

### 危険パターンの横断チェック

コンペとACEボールで見つかった不具合と同型のものが他にないかを調べた。

| パターン | 結果 |
|---|---|
| スナップショットで state 全体を置き換える `setState` | 意図的な全リセット1箇所のみ。他は全て関数形 |
| `setState` の更新関数の中で ref を書き、直後に読む | 該当なし（ACEボールの1件を修正済み） |
| 巻き戻せないモジュール変数 | tanaka / onizuka の `state`（snapshot/restore で対応済み）、`ssAchieved`（ラウンド終了時のみ書き込みなので対象外）、`currentEventConsults`（毎ビート再設定） |
| 削除したレガシー画面への残存参照 | 0件（コメント内の記述のみ） |
| タイマーの解放 | アンマウント時に `clearTimeout` あり |
| 二重タップ防御 | `if (!choosing) return` ＋ `setChoosing(false)`、ミニゲームは `swingLockedRef` |

### 修正: コンソールに毎描画で出ていたエラー

`HoleMap` と `PuttGreenView` の `<G x={} y={} scale={}>` を、react-native-svg 15 が web で `transform-origin` という無効な DOM 属性に変換し、React が毎描画で警告を出していた。標準の `transform="translate(x,y) scale(s)"` に書き換えて解消（描画結果は変わらないことを実機で確認）。

### 未修正（報告のみ）

**1. 摩耗（Wear）システムが丸ごと未接続 — `src/logic/wear.ts` 131行**

設計は完成しているが、どこからも呼ばれていない。`export` 9件すべてが未使用。

ファイル冒頭のコメントによれば、本来はこう効く予定だった。

- 自己抑圧系タグ（`self_suppress` / `over_adjust` / `over_flatter` / `forced_laugh`）で上昇、誠実系タグ・ラウンド完了・契約で減少
- 閾値 40/70/90 で mismatch率上昇・insight 抑制・スコア減少
- 70+ で内なる声、60+ で ACE ヒント

store 側にも `wear: 10` が永続化され `getWear` / `addWear` が公開されているが、画面からの呼び出しはゼロ。**mismatch と insight は「相手を読む」中核の仕組みなので、これが繋がっていないのは失われた機能**にあたる。繋ぐか外すかは要判断。

**2. その他の未使用 export**

| 場所 | 名前 |
|---|---|
| `engine.ts` | `isBadChoice`、`getReactionText` |
| `lunchMiniGame.ts` | `applyLunchMiniGame` |
| store | `refillAceBalls`、`unlockCharacter`（画面からの呼び出し0） |
| `engine.ts` | `calcEntertainScore`（自ファイル内でのみ使用。export 不要） |

**3. 45字以上の選択肢の最悪率 44%**

偶然の水準33%より高いが、該当は9件のみで統計的に意味のある差ではない。D-5 の主眼だった「！！」は13%まで落ちている。

---

## 全体の詳細バグチェック（ここまでの総点検）

クラッシュや進行不能は見つからなかった。出てきたのはすべて「書かれているのに使われていない」類。

### 1. 摩耗（Wear）システムが完全に未配線 — 131行

`src/logic/wear.ts` は設計・実装ともに完成しているが、**どこからも import されていない**。

| 実装済みの機能 | 内容 |
|---|---|
| `calcWearDelta` | `self_suppress` +2 / `over_adjust` +2 / `over_flatter` +4 / `forced_laugh` +4、`honesty`・`sportsmanship` で −1 |
| `getWearMismatchBonus` | 40以上で +0.05、70以上で +0.10（相手の反応がチグハグになる） |
| `getWearScoreMultiplier` | 90以上で entertainScore ×0.95 |
| `shouldSuppressInsight` | 70以上で20%、90以上で50%の確率で洞察を抑制 |
| `rollInnerVoice` | 70以上で15%、90以上で30%で内なる声（6種） |
| `getWearHint` | 60以上でエースに相談したときのヒスト（2種） |
| `WEAR_ROUND_END` / `WEAR_CONTRACT` / `WEAR_ACE_ROUND` | −3 / −5 / −15 の回復量 |

ストア側にも `wear`（初期値10）・`getWear`・`addWear` があり **localStorage に永続化までされている**が、`addWear` / `getWear` の呼び出し元はゼロ。値は10から一切動かない。

「接待で自分を殺し続けると摩耗し、誠実さと休息で回復する」という軸がまるごと眠っている。

### 2. キャラ別の台詞 170行が読まれていない

`Character.reactionLines`（21キャラ全員が保持・合計170行）は、**型宣言以外に参照が1件もない**。

`insight.ts` が文書化しているセリフ決定の優先順位のうち、

| 手順 | 内容 | 状態 |
|---|---|---|
| 1 | `speechOverride` | 動作 |
| 2 | `character.mismatchSpeechLines[rank]` | **実データ 0/21 → 常に空振り** |
| 3 | `character.speechLines[rank].default` | **実データ 0/21 → 常に空振り** |
| 4 | `speechLineTemplates[speechStyleId][rank]` | ここが実質の本番 |
| 5 | `speechStyles.sampleLines` | 到達しない |

つまり `reactionLines` は手順に入っておらず、`speechLines` / `mismatchSpeechLines` は型だけあって中身がない。C-5（`Choice.speech` 未設定1,112件）と同じ根の話で、**セリフは「キャラ別」ではなく「口調スタイル別」で出ている**。

ただしスタイル側は健全だった。

- 18スタイル × 各24行、21キャラ全員がテンプレートを持つ
- 手順5（弱いフォールバック）に落ちるキャラは0人
- 同じスタイルを共有しているのは3組だけ（坊っちゃん/ハジメ、中村/篠原、大門/鷹宮）

### 3. その他の未使用

| 対象 | 規模 | 状況 |
|---|---|---|
| `src/data/lunchMenu.ts` | 50行 | 冒頭コメントで自ら「`lunchMiniGame.ts` の `menuItems` を使う」と書いた上で、参照ゼロ |
| `Character.role` | 21件 | 画面に一切出ない |
| `settings.bgmEnabled` / `sfxEnabled` | — | 永続化されているが、音のライブラリ自体が未導入（`expo-av` なし）。設定UIもない |

### 4. 陳腐化したコメント

- `GameScreenSimple.tsx:189`「ACE ラウンドはコース未定義のため、id 1/2 ラウンド時のみ表示」
- `holeLayouts.ts:4`「各ラウンド（田中=1, 鬼塚=2）ごとに 9 ホール分のコース構成を定義」

実際は**21キャラ全員に9ホール分のレイアウトが揃っている**（実測）。コメントだけが初期状態のまま。

### 5. 健全だったもの

| 検査 | 結果 |
|---|---|
| `holeLayouts` の網羅 | 21/21キャラ × 9ホール、欠落0 |
| 朝イチのショット | 21/21キャラ分 |
| キャラの必須フィールドの空 | 0件 |
| キャラ id の重複・欠番 | 重複0 / 1〜21 欠番なし |
| セリフのフォールバック落ち | 0人 |

### 掃除（ステップ2）— 死んだコード 356行を削除

「未使用」を**死んだコード**と**未使用の文章資産**に分け、前者だけを削除した。

**削除したもの**

| 対象 | 行数 | 死んでいた理由 |
|---|---|---|
| `engine.getReactionText` | 304 | セリフ生成が `insight.ts` の系統に置き換わった残骸 |
| `lunchMiniGame.applyLunchMiniGame` | 31 | 画面が `calcLunchResult` を直接呼ぶ形になった |
| `gestures.swapGestureClass` | 11 | 仕草を常に本音のままにした改修（E-2 の読み合い整理）で不要に |
| `engine.isBadChoice` | 10 | 呼び出し元なし |

**export だけ余っていたもの**（自ファイル内でしか使っていないので `export` を外した）

`calcEntertainScore` / `positiveGestures` / `neutralGestures` / `negativeGestures` / `ACE_UNLOCK_CONTRACTS` / `buildGenericMorningShotEvent`

**死んだフラグ**

`GameState.aceUsedThisRound` — 型と2箇所の初期化のみで読み手ゼロ。最終パットのゲートを外したときの残骸。型ごと削除。

**陳腐化コメント2件**

「コースは id 1/2 のみ定義」と書かれていたが、実測では21キャラ全員に9ホール分ある。`GameScreenSimple.tsx` と `holeLayouts.ts` の記述を実態に合わせた。

**削除しなかったもの（文章資産なので活かす方針）**

| 対象 | 規模 | 判断 |
|---|---|---|
| `logic/wear.ts` | 131行 | ステップ1で配線する |
| `Character.reactionLines` | 170行 | ステップ3で配線する |
| `Character.role` | 21行 | `hint` とも `targetHint` とも違う独自の文章。ステップ3で表示する |
| `data/lunchMenu.ts` | 50行 | 自身のコメント通り「使われていない上位版」（22品・詳細フラグ付き）。将来メニューを増やすときの素材 |
| `settings.bgmEnabled` / `sfxEnabled` | — | 音のライブラリ自体が未導入。真偽値2つなので、音を入れるときの受け皿として残す |

**回帰**: 契約成功率 97.8 / 82.3 / 64.2 / 42.5 / 12.9%（前回 98.0 / 83.7 / 60.4 / 41.7 / 13.2）、共通プール到達 167/167、ハング 0 / 4200、相談ラウンド 100/100 正常終了、`tsc --noEmit` 通過。

### 眠っていた文章資産の配線（ステップ3）— `reactionLines` 170行と `role`

**`reactionLines` は何だったのか**

型は「セリフ」と読めるが、中身を全件見ると**地の文（ナレーション）**だった。

```
reactionLines: 「黒田の視線が厳しくなった。」   ← 描写
セリフ側:      「…違う。」                     ← 本人の言葉
```

実測: 全170行のうち **157行（92%）が「。」で終わる完全な文**、36行（21%）が中に「」を含む。
つまりセリフ枠には入らない。行き先は既存の**仕草枠**（これまで全キャラ共通の汎用30件だった）。

**混ぜ方の判断**

最初はキャラ別を優先する形にしたが、測ると副作用が出た。

| 方式 | キャラ別の採用率 | 1キャラ1ランクの文の種類 |
|---|---|---|
| キャラ別を優先 | 100% | **2種**（汎用30件が全部出番を失う） |
| 単純に足し合わせる | 17% | 12種（キャラ別がほぼ出ない） |
| **確率60%でキャラ別**（採用） | **60%** | **12種** |

キャラ別だけにすると、死んだ資産（170行）を活かす代わりに別の資産（30行）を殺し、
しかも同じ文が2種で回るので繰り返しが目立つ。`NARRATION_RATE = 0.6` で両方を残した。
実測 19,088回で キャラ別 60% / 汎用 40%、文の種類は 10 → 12 種に増えた。

**建前ターンの「」二重問題**

本音を隠すターンは、吹き出しが建前・地の文が本音になる。地の文の21%が「」を含むため、
そのまま出すと画面に鍵括弧が2組並び、**仕込んだ食い違いではなく文章の破綻に見える**。
隠すターンだけ「」を含む行を除外した。実測 4,841回の建前ターンで二重発生 **0件**。

**表示形式**

地の文は完結した文なので、汎用の仕草のように `*…*` で囲むと不自然になる。
`ReactionPlan.gestureIsNarration` を足して表示側で分岐した。実機で両方確認:

- 地の文 → 「田中の眉が少し寄った。」（囲みなし）
- 汎用 → 「\*クラブを軽く掲げた\*」（囲みあり）

**`role`**

`hint`（攻略の助言）とも `targetHint`（好みの示唆）とも別の、性格そのものの一文。
プロフィール画面の座右の銘の下に置いた。実機確認: 田中で「堅実で数字に厳しい。誠実さを重んじる。」

**回帰**: 契約成功率 98.6 / 81.7 / 62.5 / 41.9 / 12.2%（前回 97.8 / 82.3 / 64.2 / 42.5 / 12.9）、
共通プール到達 167/167、ハング 0 / 4200、空文字の仕草 0件、`tsc --noEmit` 通過。

### 摩耗システムの配線（ステップ1）— 設計をやり直してから配線した

`wear.ts` 131行は「設計済み・未配線」に見えたが、測ると**配線しても動かない**ことが分かった。
書かれていた通りに繋ぐのをやめ、成り立つ形に再設計してから入れた。

#### 動かなかった理由（配線前の測定）

| 測ったこと | 結果 |
|---|---|
| 摩耗が上がるタグ4つ（`self_suppress` `over_adjust` `over_flatter` `forced_laugh`）の使用数 | **選択肢1,034件中 0件** |
| 摩耗が下がるタグ（`honesty` `sportsmanship`）の使用数 | 174件 |
| そのまま配線した場合 | 初期値10から下がる一方で0に張り付き、永久に何も起きない |

既存タグ（おだて・絶賛・過剰な世話・沈黙・無難＝上げ／正直＝下げ）に読み替えても動かなかった。

| プレイの型 | 1ラウンドの摩耗 | 20ラウンドで40に届くか |
|---|---|---|
| 勝つ手を選び続ける | −4.9 | 届かない |
| 正直に寄せる | −7.1 | 届かない |
| おだて一本（契約成功39%） | +8.8 | 9ラウンド目 |

「勝ちながら摩耗する」のは 4/21人だけ。21人と契約する一周では一度も0から動かない。

**より本質的な問題**: 摩耗が上がる選択の creep 平均は **7.71**、下がる選択は 0.03（相関 r=0.445）。
creep の遅い二重版であり、動くときは既に負けているプレイヤーを追い打ちするだけだった。

#### 再設計 — creep と別の軸にする

```
creep = 相手が引いた       （やりすぎ・相手視点・1ラウンドで完結）
摩耗  = 相手に合わせて通った （自分を殺した・自分視点・ラウンドを越える）
```

**迎合して失敗したときは加算しない**。そこは creep の担当なので、二重に罰さない。
「通った迎合」だけを数えると creep との相関は **r=0.445 → 0.076**、
摩耗が上がる選択の creep 平均は **7.71 → 1.27** に落ちて別の軸になった。

溜まるタグ: `silence` +3 / `over_praise` +3 / `flattery` +2 / `over_support` +2 / `safe` +1

沈黙を最も重くしたので、**寡黙な黒田（+4.8）や重鎮の巌（+4.2）の前でも勝ちながら摩耗する**。
言葉を飲み込み続けるから。褒め殺し王の松本が +10.8 で最大、本音で当たれるミツキ・大門・桐生は +0.4〜0.8。

#### 回復量の決め方 — 逃げ道が無いと一方通行の数字になる

固定 −1 だと、摩耗45から本音で当たれる相手を12ラウンド回っても 43〜48 を行き来して下がらなかった。
逆に「迎合2以下で −5」にすると、今度は一周しても最大18でしきい値25に届かない。
5案を測って、両方成り立つ幅は1つだけだった。

```
そのラウンドの迎合が 1 以下 → -3（自分を出せたラウンド）
それ以上              → -1
相談ラウンド          → -15
契約成功              → 回復させない
```

契約で回復させないのは、迎合で勝った代償を勝利そのものが打ち消してしまい
「接待で勝つことには値段がある」という前提が崩れるため。

**実測（25ラウンドのキャリア×3000）**

| | 到達した最大摩耗の中央値 | 25以上に届く | 35以上 |
|---|---|---|---|
| 上手いプレイ | 41 | 86% | 66% |
| ほどほどのプレイ | 34 | 77% | 49% |

- 摩耗45から本音で当たれる相手を10ラウンド回ると **27** まで下がる（逃げ道が機能）
- 松本を連戦すると 20 → 70（6ラウンド）
- しきい値は 25 / 35。45に置くと到達が 42%／23% に落ち、強い内なる声3行と創業者の一言が半分以上に読まれない

#### 出口は2つだけ

creep と重なる罰（mismatch率上昇・スコア −5%・洞察の抑制）は**入れなかった**。
同じ行為に2つのゲージが反応するだけになるため。該当する3関数は削除した。

- **内なる声** — 迎合が通って摩耗が溜まったビートにだけ出す。ランダムなタイミングで出すと
  何に対する声か分からないが、直後に出せば隠しパラメータのまま原因と結果が結びつく。
  実測 摩耗25で0.33回／ラウンド、35以上で0.58回。摩耗0では出ない。
- **創業者の気づかい** — 相談ラウンドで、摩耗している相手にだけ答える前に一言添える（1ラウンド1回）

#### エースボールのやり直しへの対応

摩耗も巻き戻す必要があった。取り消した迎合の分が残ると、選び直したのに
自分を殺した記録だけが積まれる（`6299e33` で直した媚びスタック・不正カウントと同じ穴）。
`redoSnapshotRef` に摩耗とラウンド内合計を加えて復元する。

#### 検証

| | |
|---|---|
| 契約成功率 | 97.9 / 81.7 / 60.8 / 42.8 / 13.6%（前回 98.6 / 81.7 / 62.5 / 41.9 / 12.2）— 罰を入れていないので難易度は不変 |
| 共通プール到達 | 167/167 |
| ハング | 0 / 4200 |
| 相談ラウンド | 100/100 正常終了 |
| 関数単体 | 失敗した迎合 0 / 通った迎合 5 / 正直 0 / 摩耗0で声が出ない |
| 実機・内なる声 | 摩耗100で「…もう限界かもしれない。」が鍵括弧なし・薄い色で相手のセリフの下に表示 |
| 実機・失敗した迎合 | 坊っちゃんにおだてが刺さらなかったターンで摩耗が増えない（40→40） |
| 実機・創業者の気づかい | 摩耗50で相談すると「…少し疲れているようですね。無理は禁物ですよ。」が答えの前に1回だけ入る。同じ吹き出しに2行（どちらもハジメの言葉なので鍵括弧はひとつ） |
| `tsc --noEmit` | 通過 |
